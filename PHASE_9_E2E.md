# Phase 9 真实数据联调清单

> 本清单是 V1.1 Phase 9 的执行指引。需要 **2 个真实微信账号** + 已完成 Phase 8 部署。
> 预计耗时：2-3 小时（每个场景单独验收）
>
> **相关文档**：
> - [V1.1 发布计划](./家庭记账本_V1.1_发布计划与验收目标.md)
> - 前置：[PHASE_8_DEPLOY.md](./PHASE_8_DEPLOY.md)（云端部署）
> - 测试数据生成：`node scripts/gen-test-xlsx.js [输出路径]`
> - 下一步：[PHASE_10_DEVICE.md](./PHASE_10_DEVICE.md)（真机走查）

---

## 0. 联调前准备

| # | 准备 | 状态 |
|---|---|---|
| 1 | Phase 8 部署完成 | ⏳ 待你执行 |
| 2 | 2 个测试微信号（账号 A = 管理员，账号 B = 普通成员） | ⏳ 待你准备 |
| 3 | 真实随手记 xlsx 导出文件 | ⏳ 见 §5 |
| 4 | 删除测试账号旧数据 | ⏳ 见下方"清理"步骤 |

**清理旧数据**（如果 Phase 8 之后跑过开发版本）：
1. 打开云开发控制台 → 云函数 → `resetTestData` → 「云端测试」
2. 看到 `results: { users: "removed N", ... }` 表示成功
3. **生产环境严禁调用**

---

## 1. 初始化场景（3 个）

### 1.1 首次登录仅创建一个默认账本

**步骤**：
1. 账号 A 首次打开小程序
2. 观察：
   - 顶部"待处理邀请"面板是否出现
   - 默认账本"我的家庭账本"是否自动创建
   - 默认分类（餐饮/超市等）是否注入
   - 默认账户（现金）是否注入
   - 7 天邀请码是否生成（不自动弹出分享）

**通过标准**：
- ✅ 重复打开 3 次，默认账本不重复创建
- ✅ 集合 `families` 文档数 = 1
- ✅ 集合 `family_members` 文档数 = 1（账号 A 是 admin）
- ✅ 集合 `categories` 文档数 ≥ 15（基础 10 个一级 + 几十个二级）
- ✅ 集合 `accounts` 文档数 ≥ 1

**云函数调用参考**：
```js
// 打开云开发控制台 → ledgerFunctions → 「云端测试」→ 输入：
{ "action": "initUser" }
// 期望：{ success: true, user: {...}, family: {..., adminName: ..., role: "admin"} }
```

### 1.2 重复打开幂等

**步骤**：
1. 账号 A 关闭小程序
2. 重新打开 → 等待初始化完成
3. 重复 3 次

**通过标准**：
- ✅ `families` 文档数仍 = 1（不重复创建）
- ✅ `family_members` 文档数仍 = 1
- ✅ `initialization_locks` 集合有 `account-A-openid` 文档
- ✅ 第二次以后 `initUser` 返回的 `family._id` 一致

### 1.3 邀请码生成

**步骤**：
1. 账号 A 在「我的」→「家庭管理」
2. 看到 7 天有效期的邀请码

**通过标准**：
- ✅ `family_invites` 集合有 1 个 status=active 的文档
- ✅ `expiresAt` = 创建时间 + 7 天
- ✅ 分享按钮可用（onShareAppMessage 返回 path 含 inviteCode）

---

## 2. 邀请加入场景（2 个）

### 2.1 邀请码 + 普通成员加入

**步骤**：
1. 账号 A 在「家庭管理」点分享 → 复制路径（含 inviteCode）
2. 账号 B 扫描小程序码或从分享卡片进入
3. 账号 B 看到「加入家庭账本」面板
4. 账号 B 点「确认加入」

**通过标准**：
- ✅ 账号 B 确认前：`family_members` 文档数仍 = 1（无 B 的成员关系）
- ✅ 账号 B 确认后：`family_members` 文档数 = 2
- ✅ 账号 B 看到「我的家庭账本」的所有数据
- ✅ 账号 A 在「家庭管理」看到 B 出现
- ✅ `family_invites` 仍 status=active（同一邀请码支持多人）

### 2.2 重复加入幂等

**步骤**：
1. 账号 B 已经在账本内
2. 账号 B 再次扫描同一邀请码
3. 看到「你已在该账本中」提示
4. 选「切换到此账本」

**通过标准**：
- ✅ `family_members` 文档数仍 = 2（不创建 B 的第二条）
- ✅ 当前账本切换到该账本
- ✅ 提示文案 = "你已在该账本中"

### 2.3 邀请码撤销

**步骤**：
1. 账号 A 在「家庭管理」点「撤销邀请码」→ 确认
2. 看到「邀请码已撤销」

**通过标准**：
- ✅ `family_invites` 该文档 status=revoked
- ✅ 再次生成邀请码：旧文档仍 revoked，新文档 status=active
- ✅ 撤销的邀请码再用于 B 加入 → 失败并提示"邀请码无效"

---

## 3. 权限场景（3 个）

### 3.1 普通成员只能编辑/删除自己创建的账单

**步骤**：
1. 账号 A 记一笔账（成员=账号 A）
2. 账号 B 记一笔账（成员=账号 B，UI 默认填自己）
3. 账号 B 在账单页左滑 A 的账单 → 应看到「删除」按钮但**不可点**
4. 账号 B 点 A 的账单 → 不可编辑（账单详情不显示编辑/删除按钮）

**通过标准**：
- ✅ A 的账单：账号 B 左滑不出现删除按钮，或点了提示"无权操作"
- ✅ A 的账单：账号 B 详情页不显示编辑/删除按钮
- ✅ A 的账单：账号 B 在 addBill 选账本后看不到「编辑」入口
- ✅ B 的账单：账号 B 自身左滑有删除按钮，能编辑

### 3.2 管理员可操作所有账单

**步骤**：
1. 账号 A 在账单页左滑 B 的账单 → 看到删除按钮
2. 账号 A 点 B 的账单 → 详情页显示「编辑账单」「删除账单」

**通过标准**：
- ✅ A 能删除 B 的账单
- ✅ A 能编辑 B 的账单
- ✅ `creatorOpenId` 始终是 B 的（`is_admin` 不会改写创建者）

### 3.3 伪造 familyId 被拒绝

**步骤**：
1. 在云开发控制台 → 云函数 → `accountingFunctions` → 云端测试
2. 账号 B 的 openid 上下文，调用 `createBill` 传 `familyId: "fakeFamilyId"`

```js
{
  "action": "createBill",
  "familyId": "fakeFamilyId",
  "type": "expense",
  "amount": "1.00",
  "category1": "餐饮",
  "category2": "午餐",
  "date": "2026-08-18 12:00",
  "account": "现金",
  "memberId": "m-member"
}
```

**通过标准**：
- ✅ 返回 `{ success: false, message: "你不是该账本成员" }`
- ✅ `bills` 集合无新文档

---

## 4. 管理员转让场景（1 个）

**步骤**：
1. 账号 A 在「家庭管理」点账号 B 行的「移交管理员」
2. 二次确认 → 提交
3. 账号 A 自动变为普通成员
4. 账号 B 变为管理员

**通过标准**：
- ✅ A 的 `family_members` 文档 role=member
- ✅ B 的 `family_members` 文档 role=admin
- ✅ `families` 文档 `adminOpenid` = B 的 openid
- ✅ A 再打开小程序，看到「成员」标识
- ✅ B 看到「管理员」标识和所有管理员操作入口（分类/账户/预算/导入等）

**云函数**：`transferAdmin` 必须拒绝转让给自己（memberId==admin.openid 时失败）

---

## 5. 数据导入场景（3 个）

### 5.1 真实 xlsx 端到端

**准备**：从随手记 App 导出 xlsx（含"支出"和"收入"两个 Sheet）

**步骤**：
1. 账号 A 在「我的」→「数据导入」
2. 选 xlsx 文件 → 「确认导入」
3. 观察：
   - 预览显示前 N 条
   - 成员归属映射（昵称→账本成员）
   - 导入完成后弹窗显示「成功 N 条 / 跳过 N 条」

**通过标准**：
- ✅ 支出 + 收入两个 Sheet 都正确导入
- ✅ 字段映射正确：日期/类型/分类/账户/金额/成员/商家/备注
- ✅ 唯一昵称自动匹配到账本成员
- ✅ 同名昵称归入管理员
- ✅ 未匹配的成员归入管理员
- ✅ 重复账单被跳过（基于 importFingerprint）
- ✅ 不存在的分类/账户自动创建
- ✅ `bills` 文档带 `importBatchId`

### 5.2 导入撤销

**步骤**：
1. 导入完成后，点「撤销刚刚导入」
2. 二次确认

**通过标准**：
- ✅ 仅删除该批次导入的账单
- ✅ 原有的账单不动
- ✅ 批次内自动创建且未被其他账单使用的分类/账户被清理
- ✅ `bills` 集合该 batchId 的文档数 = 0
- ✅ 操作记录写入 `operation_logs`

### 5.3 导入中途失败

**步骤**（难复现，需要构造）：
- 准备一个超大 xlsx（> 1000 条）触发事务超时
- 或在导入过程中关闭网络

**通过标准**：
- ✅ 失败响应包含 `batchId` 和 `imported` 数量
- ✅ 提示"已导入 N 条后中断，可撤销"
- ✅ 撤销按钮可用，撤销只删本批次

---

## 6. 统计场景（2 个）

### 6.1 月度统计准确性

**步骤**：
1. 在账单页加几笔本月支出（如 100/200/300）+ 1 笔收入（500）
2. 切到「统计」Tab
3. 选择「按月」+ 当前月
4. 观察：
   - 支出 = 600
   - 收入 = 500
   - 结余 = -100
   - 分类占比饼图显示 3 个分类
   - 趋势图显示每日数据

**通过标准**：
- ✅ 三个数字与账本页加总之和一致
- ✅ 饼图按金额降序排列
- ✅ 切到「按年」和「自定义范围」数据正确
- ✅ 切换成员/账户过滤器数据更新

### 6.2 未来账单不计入当前月

**步骤**：
1. 在账单页加一笔未来日期的账单（如 2026-09-15，1000 元）
2. 当前月（2026-08）统计应不包含这笔

**通过标准**：
- ✅ 当前月统计支出不含未来账单
- ✅ 自定义范围 2026-08-01 ~ 2026-09-30 时包含
- ✅ 首页「最近账单」包含（按日期倒序）
- ✅ 预算（按月）不计算未来账单

---

## 7. 账号注销场景（2 个）

### 7.1 普通成员注销

**步骤**：
1. 账号 B 在「我的」→ 底部「注销账号」
2. 二次确认「确认注销」
3. 重新登录 B → 看到欢迎页（视为新用户）

**通过标准**：
- ✅ B 的 `users` 文档被清空（personalInfo 删除）
- ✅ B 在所有账本中的 `family_members` 文档 status=left
- ✅ 历史账单中 B 的成员信息保留，昵称显示"已注销用户"
- ✅ B 重新登录看到欢迎引导
- ✅ B 加入新账本时是新成员（无遗留关系）

### 7.2 管理员需先转让

**步骤**：
1. 账号 A（管理员）点「注销账号」
2. 看到「你仍管理 X，请先在家庭管理中转让管理员」

**通过标准**：
- ✅ A 看到转让提示，不能直接注销
- ✅ 跳转到家庭管理页能完成转让
- ✅ 转让后再次点注销 → 进入正常流程

---

## 8. 解散与恢复场景（1 个）

**步骤**：
1. 账号 A 把 B 移出账本
2. 账号 A 点「解散账本」→ 二次确认
3. 重新打开小程序

**通过标准**：
- ✅ `families` 文档 status=dissolved
- ✅ 所有 `family_members` 文档 status=dissolved
- ✅ 所有 `family_invites` 文档 status=revoked
- ✅ 旧邀请码再用于加入 → 失败
- ✅ 直接伪造 `familyId` 访问 → 失败
- ✅ 解散后操作记录写入 `operation_logs`（含 `family.dissolve`）

---

## 9. 并发场景（4 个，需 2 个账号同时操作）

### 9.1 成员并发加入

**步骤**：
1. 准备 2 个未在账本内的测试号 C 和 D
2. 同时扫描同一邀请码 + 确认加入

**通过标准**：
- ✅ C 和 D 都成功加入
- ✅ `family_members` 新增 2 条（C + D）
- ✅ 成员数显示正确

### 9.2 遗留成员收敛

**步骤**：
1. 手动在云开发控制台给某用户插入遗留的随机 ID 成员文档
2. 该用户用正规流程加入账本

**通过标准**：
- ✅ 同一 openid 只保留 1 条 status=active 记录
- ✅ 遗留文档被标记 status=superseded

### 9.3 账单并发编辑

**步骤**：
1. 账号 A 和 B 同时打开同一账单的编辑页
2. A 先点保存（version=1 成功，version=2）
3. B 后点保存（version=1 失败，提示"账单已被修改"）

**通过标准**：
- ✅ A 成功，账单 version=2
- ✅ B 失败，提示文案准确
- ✅ B 看到的是 A 已修改后的金额

### 9.4 管理员转让并发

**步骤**：
1. 账号 A 同时对 B 和 C 发起转让
2. 观察：仅 1 个成功

**通过标准**：
- ✅ 仅 1 个请求成功
- ✅ 失败的请求返回"已被其他操作抢先"
- ✅ 账本始终只有 1 个 admin

---

## 10. 50 人上限场景

**步骤**（耗时）：
- 准备 50 个测试号，全部加入同一账本
- 第 51 个尝试加入

**通过标准**：
- ✅ 前 50 个都成功
- ✅ 第 51 个失败并提示"账本成员已满"

---

## 11. 时区场景

**步骤**：
1. 跨日测试：在 23:00 记一笔，第二天 01:00 切到 23:00 那天的统计
2. 月末测试：8 月 31 日 23:00 记一笔，9 月 1 日 00:00 查看 8 月统计

**通过标准**：
- ✅ 按 Asia/Shanghai 时区归类
- ✅ 跨日账单归到实际日期
- ✅ 跨月账单归到 8 月，不归到 9 月

---

## 12. 联调结果记录模板

每完成一个场景，在「联调日志」追加：

```markdown
### 9.1 成员并发加入
- 日期：2026-08-19
- 操作人：A / B
- 结果：✅ 通过 / ⚠️ 部分通过 / ❌ 失败
- 问题：（如有）
- 修复：（如有）
```

---

## 联调日志

> 实施时追加：

- 2026-08-19：（待你执行）
