# 家庭记账本云开发部署与验收清单

本文档是测试环境和发布前的执行清单。所有业务集合必须保持“仅创建者可读写”，前端不得直接访问数据库。

## 1. 部署顺序

1. 在微信开发者工具确认 `miniprogram/app.js` 的云环境 ID 与当前测试环境一致。
2. 在云开发控制台创建以下集合，并把权限设为“仅创建者可读写”：`users`、`families`、`family_members`、`family_invites`、`categories`、`accounts`、`bills`、`budgets`、`bill_preferences`、`operation_logs`、`initialization_locks`。
3. 上传并部署 `ledgerFunctions`，再上传并部署 `accountingFunctions`。后者部署前须在其目录执行依赖安装，使 `xlsx` 随云函数上传。
4. 清除两个测试账号在测试环境中的旧数据，分别使用管理员账号和普通成员账号重新初始化。
5. 上传小程序代码，在开发者工具和真机分别执行下方验收。

## 2. 建议索引

| 集合 | 索引字段 | 用途 |
|---|---|---|
| `family_members` | `familyId, openid, status` | 成员与权限校验 |
| `family_members` | `openid, status` | 初始化和账本列表 |
| `bills` | `familyId, deleted, date` | 首页、账单分页和统计 |
| `bills` | `familyId, importBatchId, deleted` | 导入撤销 |
| `bills` | `familyId, importFingerprint, deleted` | 导入去重 |
| `categories` | `familyId, type, parentId, name` | 分类校验和管理 |
| `accounts` | `familyId, name` | 账户校验和管理 |
| `budgets` | `familyId, month` | 月预算 |
| `operation_logs` | `familyId, createdAt` | 操作日志分页 |
| `family_invites` | `code, status` | 邀请码验证 |
| `initialization_locks` | `openid` | 默认账本初始化幂等锁 |
| `families` | `adminOpenid` | 管理员账本查询 |
| `operation_logs` | `familyId, action, targetId` | 解散/注销日志去重 |

## 3. 双账号验收

| 场景 | 通过标准 |
|---|---|
| 初始化 | 管理员首次登录仅创建一个默认账本、默认分类、默认账户和 7 天邀请码；重复打开不重复创建。 |
| 邀请加入 | 普通成员通过邀请码先看到账本名、管理员和成员数；确认前没有成员关系和默认账本；确认后成为普通成员并切换账本。 |
| 权限 | 普通成员只能编辑、删除自己创建的账单；管理员可以操作所有账单和配置；直接伪造 `familyId` 必须被云函数拒绝。 |
| 管理员转让 | 管理员不能把权限转给自己；只能转给当前账本的有效普通成员，转让后账本始终只有一名管理员。 |
| 账本切换 | 切换后首页、账单、统计、预算、分类和账户都只读取新账本；失败切换不改变当前账本缓存。 |
| 账单 | 金额、分类、账户、日期、成员字段由云端校验；编辑/删除使用版本号冲突校验；删除后统计和预算立即排除。 |
| 分类账户 | 未使用项目物理删除；已使用项目停用；最后一个可用账户和一级分类最后一个可用二级分类不可删除或停用。 |
| 预算 | 无预算、80% 预警、100% 超支、删除预算和未来月份预算均正确显示。 |
| 导入导出 | 导入预览展示成员映射；唯一昵称才自动匹配，同名昵称明确归入管理员；重复账单跳过；撤销只影响该批次；导出全部和当前筛选结果均能用 Excel 打开。 |
| 时区 | 上海跨日、月末和未来账单下，首页/预算当前月默认不计算未来账单；显式未来范围统计包含未来账单。 |
| 账号注销 | 普通成员注销后退出全部账本、资料和偏好被清理；管理员有其他成员时必须先转让，有且仅有本人时可先解散；历史账单、搜索和导出只显示“已注销用户”。 |
| 解散与恢复 | 账本解散后，旧页面缓存、直接伪造 familyId 和旧邀请码均不得再读取、写入或重新加入；注销请求在网络中断后重试必须只完成剩余步骤，不恢复任何已撤销访问权限。 |
| 邀请信息最小暴露 | 邀请确认返回只包含账本名称、管理员昵称、成员数和状态，不包含 familyId；分享路径只携带邀请码。 |
| 解散中断恢复 | 在成员状态更新成功、账本状态更新失败的模拟中断后，再次执行解散必须成功收敛为 families.status=dissolved、成员不可访问、邀请码已撤销，且 family.dissolve 审计记录不重复。 |
| 成员并发加入 | 两个用户同时确认同一邀请码均成功成为普通成员；第 51 人被拒绝；同一用户重复确认幂等且不创建第二条成员关系。 |
| 遗留成员收敛 | 存在随机 ID 遗留成员文档的用户再次加入时，服务端收敛为单条规范成员关系，不产生重复 active 记录或权限歧义。 |
| 账单并发编辑 | 对同一账单同时发起编辑，仅一个请求成功并返回新版本号；另一个请求因版本冲突被拒绝并提示刷新。 |
| 管理员转让并发 | 管理员同时发起两次转让给不同成员，仅一个请求成功，账本始终只有一名管理员。 |

## 4. 真机视觉验收

- 覆盖 iPhone 刘海屏、小屏 Android 与大屏 Android；检查自定义导航、底部 TabBar、FAB、底部选择器和键盘顶起。
- 检查长账本名、长昵称、长备注和大金额，文本不可重叠、截断失控或撑破布局。
- 所有空态、错误态、加载态、图片加载失败和弱网恢复均可理解、可操作。
- 在品牌 PNG/WebP 生成后，逐项核验 Alpha、实际比例、DPR 清晰度、体积和真机裁切；不通过时重新生成，不用 CSS 拉伸掩盖。

## 5. 当前未完成验收

- 此仓库已通过微信开发者工具 CLI preview 编译；尚未执行云函数上传部署、云数据库索引创建、真实 xlsx 往返或真机双账号验收。
- 当前会话没有可调用的 ImageGen 工具，`miniprogram/images/brand/` 仅保留接入位置和页面降级逻辑，最终品牌资源尚待生成与验收。
- 统计页已接入官方 ec-canvas/ECharts 运行组件，但尚未在微信开发者工具或真机验证 Canvas 初始化、图表非空、筛选更新和触控表现。
- 搜索已覆盖当前账本全部未删除账单；大数据量搜索的云端耗时和索引策略仍需压测。导入已兼容 Date、Excel 序列日期和常见日期文本，但真实随手记文件往返仍未验收。
- 开发者工具首次预览报告源码 2310KB 超过 2MB；排除无引用模板图片后 preview 成功，主包实测 1,217,938 Byte。最终加入品牌资源后必须再次确认仍低于 2MB。
- 开发者工具运行时曾发现缺失品牌图 500 和 for-of Babel runtime 缺失；已改为无资源时不请求图片并移除前端 for-of，必须清空控制台后重新编译确认不再出现。
