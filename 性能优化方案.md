# 家庭记账本小程序 — 性能优化方案

> 本文档基于对 `miniprogram-1` 项目的代码审计结果，按影响面 × 实施成本拆分成可独立交付的优化步骤。  
> 每一步都给出**现象 → 根因 → 改法 → 验收指标**，执行时按顺序做即可。
> 优先级说明：🔴 必修（影响明显，1–2 天可做）、🟠 推荐（明显改善，2–4 天）、🟢 锦上添花（长期可持续优化）。

---

## 目录

- [Step 0：现状速览（性能基线）](#step-0现状速览性能基线)
- [Step 1：云函数加内存缓存（最高 ROI）](#step-1云函数加内存缓存最高-roi-)
- [Step 2：合并 getStats 的 N+1 查询](#step-2合并-getstats-的-n1-查询)
- [Step 3：精简云函数返回字段](#step-3精简云函数返回字段)
- [Step 4：把 getStats 拆为「概览」+「详情」两档](#step-4把-getstats-拆为概览详情两档)
- [Step 5：首页并行加载 + 骨架屏](#step-5首页并行加载--骨架屏)
- [Step 6：记账保存的关键路径瘦身](#step-6记账保存的关键路径瘦身)
- [Step 7：账单/统计列表的本地缓存与分页优化](#step-7账单统计列表的本地缓存与分页优化)
- [Step 8：ECharts 体积与按需加载](#step-8echarts-体积与按需加载)
- [Step 9：账本切换的全局广播与本地数据复用](#step-9账本切换的全局广播与本地数据复用)
- [Step 10：图片资源与首屏体积](#step-10图片资源与首屏体积)
- [Step 11：WXML 渲染层优化（hidden / 提前算好展示字段）](#step-11wxml-渲染层优化hidden--提前算好展示字段)
- [Step 12：setData 合并与按需刷新](#step-12setdata-合并与按需刷新)
- [Step 13：上线前的验收清单](#step-13上线前的验收清单)

---

## Step 0：现状速览（性能基线）

通过阅读 `app.js`、`pages/*/index.js`、`cloudfunctions/accountingFunctions/index.js`、`cloudfunctions/ledgerFunctions/index.js`，定位到的核心瓶颈：

### 云函数侧（accountingFunctions）
- `listBills` 每次都会额外查 `family_members` 整表（`anonymizeCancelledBillMembers` → `getFamilyMembers`），形成 **N+1**。
- `getStats` 走 `getAllBills` 全量分页，无日期过滤即把全账本所有 `bills` 拉到内存聚合，数据量一大就慢到秒级。
- `listCategories / listAccounts / listMembers / getBillPreferences` 四个独立云函数，几乎每个页面都会一次性全打。
- 没有任何缓存层，所有请求都直接打云数据库。

### 前端侧
- `app.js` 的 `initialize()` 走 `ledgerFunctions: initUser`，含 **多次 `getFamilyMembers` + `ensureFamilySeedData`**，冷启动时它阻塞所有 `onShow`。
- `pages/index/index.js onShow` 串行依赖 `app.ensureInitialized()`，然后并发 `listBills + getStats`。
- `pages/addBill/index.js onLoad` 发起 **4 个并发**（listCategories + listAccounts + listMembers + getBillPreferences），用户感知「打开慢」。
- 几乎所有页面 `onShow` 都 `await app.ensureInitialized()`，且每次 `onShow` 都 `setData({loading:true})`，产生可见白屏。
- `pages/stats/index.js` 用 `import * as echarts from "../../ec-canvas/echarts"` 在页面级同步导入 ECharts 全量包，主包变大、首屏变慢。
- `bills / stats / addBill` 等页面 `onShow` 全部重新拉数据，没有"未变更就不重拉"的判断。
- `bills` 列表用 `amount/100.toFixed(2)` 在前端 map，账单多时 CPU 也吃紧。

### 资源侧
- `app.json` 已开 `lazyCodeLoading: "requiredComponents"`，但 `ec-canvas/echarts` 仍是页面级同步 import，未拆异步分包。
- 4 张 TabBar 图标是本地 PNG，体积可控，但 `images/brand/*.png` 走网络资源首屏才加载。

> 以上即是后续 12 步要解决的问题。

---

## Step 1：云函数加内存缓存（最高 ROI） 🔴

**现象**：打开首页 → 冷启动 `initUser` + `listMembers` + `listBills` + `getStats` 共 **≥5 次**云函数往返，每跳 Tab 又再触发。
**根因**：每个云函数无缓存，每次都直查 DB；`family_members / categories / accounts` 这类账本内元数据其实极少变更（写完才变）。
**改法**：在云函数内引入"短 TTL 内存缓存"，按 `(familyId, key)` 缓存。示例（`accountingFunctions/index.js`）：

```js
// 1) 在文件顶部加一个轻量缓存
const CACHE_TTL_MS = 30 * 1000; // 元数据 30s；过期即重查
const memCache = new Map(); // key -> { value, expireAt }
const cacheGet = (key) => {
  const hit = memCache.get(key);
  if (!hit) return null;
  if (hit.expireAt < Date.now()) { memCache.delete(key); return null; }
  return hit.value;
};
const cacheSet = (key, value, ttl = CACHE_TTL_MS) => memCache.set(key, { value, expireAt: Date.now() + ttl });
const cacheDel = (prefix) => {
  for (const k of memCache.keys()) if (k.startsWith(prefix)) memCache.delete(k);
};

// 2) listMembers / listCategories / listAccounts 加缓存
const listMembers = async (event) => {
  const { familyId } = event;
  await requireMember(familyId);
  const cacheKey = `members:${familyId}`;
  const cached = cacheGet(cacheKey);
  if (cached) return { success: true, members: cached, cached: true };
  const members = await getFamilyMembers(familyId, "active");
  const payload = members.map((item) => ({
    memberId: item._id, nickName: item.nickName, avatarUrl: item.avatarUrl,
    role: item.role, joinedAt: item.joinedAt
  }));
  cacheSet(cacheKey, payload, 30 * 1000);
  return { success: true, members: payload };
};
// listCategories / listAccounts 同理，加 cacheKey + cacheGet/cacheSet。
```

**写操作后失效**（在 `createCategory / deleteCategory / setCategoryEnabled / createAccount / deleteAccount / setAccountEnabled` 的 `success` 之前）：
```js
cacheDel(`categories:${familyId}`);
cacheDel(`accounts:${familyId}`);
```

**注意**：
- 不同云函数实例内存不共享，所以单实例冷启动仍会查一次；但**同一个用户连续点 Tab** 时，30 秒内是 0 次 DB。
- 不要缓存 `bills` 类大集合（Step 4 再处理）。

**验收**：
- 首页首次进：DB 命中数从 5+ 降到 2~3。
- 在 `bills` 和 `stats` 之间切 5 次：`listMembers / listCategories / listAccounts` 三次云函数命中缓存，console 不再打印云函数 db read 计数。

---

## Step 2：合并 getStats 的 N+1 查询 🔴

**现象**：账单变多后 `stats` 页加载 2~5 秒。
**根因**：
1. `getStats` 走 `getAllBills` 全量分页 100 条查回内存。
2. 然后又调 `anonymizeCancelledBillMembers → getFamilyMembers` 再查一次 `family_members`。
3. 内存里 forEach 聚合，账单 1000 条时明显卡。
**改法（两步走）**：

### 2.1 用云数据库的 `aggregate` 在服务端聚合
```js
const getStats = async (event) => {
  const { familyId, month, year, dateStart, dateEnd, memberId, account, category } = event;
  await requireMember(familyId);
  const bounds = computeBounds({ month, year, dateStart, dateEnd }); // 你已经写好的逻辑
  const $ = db.command.aggregate;

  // 基础 where（与现有 listBills 一致）
  const match = { familyId, deleted: false };
  match.date = bounds.endExclusive
    ? $.and([$.gte(bounds.start), $.lt(bounds.end)])
    : $.and([$.gte(bounds.start), $.lte(bounds.end)]);
  if (memberId) match.memberOpenid = (await resolveMemberOpenidForFilter(familyId, memberId));
  if (account) match.account = account;
  if (category) match.$or = [{ category1: category }, { category2: category }];

  const _ = db.command; // 未来日期过滤
  const today = getShanghaiDate() + " 23:59";
  if (!includeFuture && !explicitRangeIncludesFuture && !selectedPeriodStartsInFuture) {
    match.date = _.lte(today);
  }

  const agg = await db.collection("bills").aggregate()
    .match(match)
    .group({
      _id: { type: "$type", category2: "$category2", category2Icon: "$category2Icon", date: $.substr(["$date", 0, 10]) },
      amount: $.sum("$amount")
    })
    .end();

  // 内存里按 _id.type / _id.date 还原 dailyTrend + categoryStats
  // ...
};
```

### 2.2 跳过 `anonymizeCancelledBillMembers`
`getStats` 返回的统计结果里并不需要 `member` / `memberId`，完全可以**不查 `family_members`**。删除 `await anonymizeCancelledBillMembers(...)` 这一步即可。
- 仅在 `listBills / getBill` 这些**返回账单明细**的接口里保留；这些场景下也加 Step 3 的字段精简来减少传输体积。

**验收**：
- `getStats` 的云函数平均耗时下降 50%+（用云开发控制台"日志/监控"看 `durationMs`）。
- 账单 500 条的账本，统计页从 ~3s 降到 < 1s。

---

## Step 3：精简云函数返回字段 🔴

**现象**：账单/统计一次返回大对象，移动网络下被压缩前体积偏大。
**根因**：`bills` 集合里 `creatorOpenId / memberOpenid / deletedBy / importFingerprint` 等字段虽在 `anonymizeCancelledBillMembers` 里被剔除，但返回给客户端的对象里仍然带着不少冗余（`familyId` 已知、`_id` 已暴露、`memberOpenid` 已剔除但仍有 `memberId` 等重复信息）。
**改法**：在 `listBills` 末尾显式投影：
```js
const project = (b) => ({
  _id: b._id,
  type: b.type,
  amount: b.amount,            // 分，前端展示除 100
  category1: b.category1,
  category1Icon: b.category1Icon,
  category2: b.category2,
  category2Icon: b.category2Icon,
  date: b.date,
  account: b.account,
  member: b.member,
  memberId: b.memberId,
  memberAvatarUrl: b.memberAvatarUrl,
  remark: b.remark,
  merchant: b.merchant,
  version: b.version
});
return { success: true, bills: result.data.map(project) };
```
- 同样规则应用到 `getBill / searchBills / exportBills`。
- `getStats` 只返回数字和名称，不返回整条账单。

**验收**：
- 100 条账单接口的 `response 体积` 减少 30% 以上（云开发控制台看"出网流量"）。
- 移动 4G 下拉刷新明显变快。

---

## Step 4：把 getStats 拆为「概览」+「详情」两档 🟠

**现象**：首页 `getStats` 实际只需要 `totalExpense/totalIncome/balance + today expense` 4 个数字，但当前返回全量 `expenseCategoryStats/incomeCategoryStats/dailyTrend` —— 对首屏是浪费。
**改法**：在 `accountingFunctions/index.js` 新增 `getHomeSummary`：
```js
const getHomeSummary = async (event) => {
  const { familyId, month } = event;
  await requireMember(familyId);
  const bounds = getMonthBounds(month);
  const $ = db.command.aggregate;
  const agg = await db.collection("bills").aggregate()
    .match({ familyId, deleted: false, date: $.and([$.gte(bounds.start), $.lt(bounds.end)]) })
    .group({ _id: { type: "$type", date: $.substr(["$date", 0, 10]) }, amount: $.sum("$amount") })
    .end();
  let totalExpense = 0, totalIncome = 0, todayExpense = 0;
  const today = getShanghaiDate();
  agg.list.forEach((row) => {
    if (row._id.type === "expense") totalExpense += row.amount;
    if (row._id.type === "income") totalIncome += row.amount;
    if (row._id.date === today && row._id.type === "expense") todayExpense = row.amount;
  });
  return {
    success: true,
    totalExpense: totalExpense / 100,
    totalIncome: totalIncome / 100,
    balance: (totalIncome - totalExpense) / 100,
    todayExpense: todayExpense / 100
  };
};
```

把 `pages/index/index.js` 里的 `getStats` 调用替换为 `getHomeSummary`：
```js
// loadHomeData
const [billResponse, summaryResponse] = await Promise.all([
  wx.cloud.callFunction({ name: "accountingFunctions", data: { action: "listBills", familyId: currentFamilyId, limit: 10, sort: "dateDesc" } }),
  wx.cloud.callFunction({ name: "accountingFunctions", data: { action: "getHomeSummary", familyId: currentFamilyId, month } })
]);
// 用 summaryResponse.result 直接覆盖 4 个概览数字
```

**验收**：
- 首页 `getStats` 替换后首屏时延（从 onShow 到 setData 完毕）下降 30~60%。
- 老的 `getStats` 仍然给统计页用。

---

## Step 5：首页并行加载 + 骨架屏 🟠

**现象**：进入首页看到一段 `state-panel` 全屏 loading（"正在准备你的家庭账本…"），停留时间过长。
**根因**：`onShow` 串行等待 `app.ensureInitialized()` 再 `await Promise.all([loadFamilyInfo, loadHomeData])` —— 后者又是 `listBills + getStats` 两个云函数。
**改法**：
1. **缓存初始化结果**：在 `app.js` 维护 `app.globalData.userInfo` 和 `app.globalData.currentFamily`，首屏**先读缓存渲染骨架**，`ensureInitialized` 后再补数据。
   ```js
   // app.js onLaunch 末尾
   const cachedUser = wx.getStorageSync("userInfoCache");
   const cachedFamily = wx.getStorageSync("currentFamilyCache");
   if (cachedUser) this.globalData.userInfo = cachedUser;
   if (cachedFamily) { this.globalData.currentFamily = cachedFamily; this.globalData.currentFamilyId = cachedFamily.id; }
   ```
2. **首页渲染优化**（`pages/index/index.js onShow`）：
   - 不再 `setData({ loading: true })` 阻塞全屏；改为「骨架屏展示 + 局部 `setData` 增量刷新」。
   - 拆成 3 段：
     ```js
     // 1) 立刻用缓存数据渲染（onShow 不再 await 顶层初始化）
     this.applyCachedSummary();
     // 2) 并行启动：初始化 + 数据加载
     this.refresh();
     // 3) refresh 内不抛错时静默更新；仅在 errorMessage 时才覆盖
     ```
3. **数据存缓存**：
   ```js
   // 在 setData 末尾把最近账单 + 概览数字存到 storage：
   wx.setStorageSync(`home:summary:${familyId}`, { ts: Date.now(), summary, recentBills, groupedBills });
   ```
   - 下次 onShow 先用本地缓存渲染，再后台刷新。

**验收**：
- 二次进入首页：直接看到内容，"最近账单"区域只显示一行 shimmer 占位 200~500ms 后被真数据替换。
- 测速：冷启动 → 首页有可见内容时间 < 600ms。

---

## Step 6：记账保存的关键路径瘦身 🟠

**现象**：点保存按钮要等 1~2 秒才返回，体感"卡"。
**根因**：`saveBill` 顺序是 `createBill` → `savePreferences`，且 `createBill` 是事务（多次 DB roundtrip），还顺带 `addOperationLog`。
**改法**：
1. **保存后立即跳回**：把 `createBill` 成功后立刻 `wx.navigateBack()` 并 `wx.showToast`，**`savePreferences` 改为后台 fire-and-forget**：
   ```js
   await this.callFunction("createBill", billData);
   wx.showToast({ title: "记账成功" });
   wx.navigateBack({ delta: 1, success: () => {
     // 后台保存偏好，不阻塞返回
     this.callFunction("saveBillPreferences", preferenceData).catch((e) => console.warn(e));
   }});
   ```
2. **可选：在 `createBill` 云函数里把"操作日志"做成异步**（`addOperationLog` 不要放在 `db.runTransaction` 里，写入挪到事务外 fire-and-forget）：
   ```js
   // 事务内
   const billId = await transaction.collection("bills").add({ data: billData });
   return billId._id;
   // 事务结束后
   await writeOperationLog(familyId, "bill.create", billId, { type, amount }).catch(() => {});
   ```
3. **预算联动**（如有需要）：保存后只让 `getHomeSummary` 增量拉一次（Step 4 已实现），**不重拉整张账单列表**。

**验收**：
- "保存" 按钮按下到页面返回 < 800ms。
- 偏好记录最终一致即可（不强求同一事务）。

---

## Step 7：账单/统计列表的本地缓存与分页优化 🟠

**现象**：`bills` 页拉一屏要等几秒；月内来回切月、动筛选项每次都全量重拉。
**根因**：每次 `loadBills` 都走云函数 + 全量 `anonymizeCancelledBillMembers`；`onShow` 也会重拉一次；筛选项变更无去抖。
**改法**：
1. **加一层 wx.storage 缓存**：
   ```js
   const cacheKey = `bills:${familyId}:${filterMonth}:${hash(filters)}`;
   const cached = wx.getStorageSync(cacheKey);
   if (cached && Date.now() - cached.ts < 60_000) {
     this.setData({ bills: cached.bills });
     return;
   }
   // 否则正常拉
   wx.setStorageSync(cacheKey, { ts: Date.now(), bills: data });
   ```
2. **筛选项变更做 200ms 防抖**：
   ```js
   const debounced = debounce(() => this.loadBills(), 200);
   // selectFilter / changeFilter / selectType / selectSort 都走 debounced
   ```
3. **下拉刷新时主动清缓存**。
4. **`onShow` 不必每次都拉**：仅当 `(loadedFamilyId !== familyId) || (storage.lastRefresh < now - 30s) || 下拉过` 时才拉。
5. **`anonymizeCancelledBillMembers` 也加缓存**（同 Step 1 思路）：
   ```js
   const cancelledKey = `cancelled:${familyId}`;
   const cancelled = cacheGet(cancelledKey) || await (async () => {
     const m = await getFamilyMembers(familyId);
     const set = new Set(m.filter((x) => x.status === "cancelled").map((x) => x.openid));
     cacheSet(cancelledKey, Array.from(set), 30_000);
     return set;
   })();
   ```
6. **账单/统计的 onReachBottom / loadMore** 加 200ms 节流，避免快速滚时多次空转。

**验收**：
- 同月内来回切换：第二次进入页面 < 200ms 渲染。
- 切到下个月再切回来：也是 < 200ms（命中缓存）。

---

## Step 8：ECharts 体积与按需加载 🟠

**现象**：统计页 ECharts 是页面级同步 `import`，主包变大，统计页首次进入 ~1s 才出图。
**根因**：`import * as echarts from "../../ec-canvas/echarts"` 同步打入统计页 chunk，主包基础大小因此被撑大。
**改法**：
1. **用按需引入**（仅在 `stats` 页用到）：
   ```js
   // pages/stats/index.js 顶部
   const echarts = require("../../ec-canvas/echarts");
   // 改为：把 echarts 初始化逻辑挪到一个独立 module；
   // 并配合 mp-bundle-webpack-plugin / 微信开发者工具的"按需注入"做代码拆分。
   ```
2. **改用微信原生 Canvas** 画饼图/折线（数据点 < 30），性能更好。已有 ECharts 的话，可以把 `trend` 折线用纯 Canvas 画，**仅保留饼图的 ECharts**。
3. **把 ec-canvas 改造成自定义组件异步加载**：
   - 在 `app.json` 移除显式引用，改为 `usingComponents` 时 `componentPlaceholder: "ec-canvas"` 异步占位。
   - 或在 stats 页面 `onLoad` 里 `await import()` 动态 import。
4. **预渲染占位**：`<view class="chart-card"><view class="chart-skeleton" /></view>`，等数据到再渲染 ec-canvas。

**验收**：
- 主包体积下降 ≥ 50KB（看 `编译预览` 的体积报告）。
- 统计页首次进入 → 图表出现 < 1.2s。

---

## Step 9：账本切换的全局广播与本地数据复用 🟢

**现象**：在首页切换账本后，所有页面（`bills / stats / budget / category / account`）状态还是旧账本，体感"卡住"。
**根因**：当前 `switchFamily` 只更新了 `currentFamilyId`，其他页面要等下次 `onShow` 才重拉；旧页面的 data 还在内存里，渲染时短暂错位。
**改法**：
1. **App 端增加全局事件总线**：
   ```js
   // app.js
   globalData: { ... },
   onFamilyChange(family) {
     this.globalData.currentFamily = family;
     this.globalData.currentFamilyId = family.id;
     wx.setStorageSync("currentFamilyId", family.id);
     this.initializePromise = null; // 让后续 ensureInitialized 重新跑
     // 广播
     const pages = getCurrentPages();
     pages.forEach((p) => typeof p.onFamilyChanged === "function" && p.onFamilyChanged(family));
   }
   ```
2. **每个页面实现 `onFamilyChanged`**：清空旧 data + 立即重拉需要的接口；同时清 Step 7 的本地缓存。
3. **将"当前账本"绑定进本地缓存 key**（已经做的话不需要再改）。

**验收**：
- 切账本后，账单/统计页不会出现"上家账本"残留数据。
- 切完不闪退、不白屏。

---

## Step 10：图片资源与首屏体积 🟢

**现象**：首屏有 4 张品牌 PNG，文件偏大；非 4G 网络下出图慢。
**根因**：`images/brand/*.png` 直接 `<image src=...>`，未压缩、未懒加载。
**改法**：
1. **压缩 PNG**（用 `pngquant / squoosh`）：每张 100~200KB → 30~60KB。
2. **`<image lazy-load>`**：`index.wxml` 的品牌 logo / 空状态图都加 `lazy-load="{{true}}"`。
3. **`image` 加 `mode="widthFix"` 避免首屏重排**。
4. **可选：欢迎插画用 SVG**（`welcome-guide`）—— 体积更小、可按主题色着色。
5. **tabBar 图标**：当前是 PNG，可在 `images/icons` 下确认尺寸已 ≤ 30KB；如 > 30KB，用 `tinypng` 压一下。

**验收**：
- 首屏 brand 图片加载完成的 LCP 时间下降。
- 开发者工具"网络"面板看不到 > 80KB 的图片请求。

---

## Step 11：WXML 渲染层优化（hidden / 提前算好展示字段） 🟢

**现象**：账单页几十条以上时，滑动有可感知掉帧。
**根因**：
1. WXML 中 `item.date.substring(0, 10)`、`item.amount / 100`、三元 `item.type === 'expense' ? '-' : '+'` 等表达式在每次数据变化/重渲染时都执行。
2. `bills` 列表里嵌了"日期分组"，分组本身在 JS 里 filter+find，N²。
**改法**：
1. **后端或前端预处理展示字段**：在 `listBills` 返回前就把 `displayAmount / displayDate / displaySign` 算好（或者在 `onShow` 拉到后只算一次），WXML 里直接 `{{item.displayAmount}}`。
2. **改用 `<block wx:if>` + 一次性 list**：
   ```js
   // 一次 reduce 出 group + bills 顺序
   const flat = [];
   grouped.forEach((g) => { flat.push({ _type: "head", title: g.title, sub: g.sub }); g.bills.forEach((b) => flat.push({ _type: "bill", ...b })); });
   this.setData({ flatList: flat });
   ```
   WXML：
   ```html
   <block wx:for="{{flatList}}" wx:key="key">
     <view wx:if="{{item._type === 'head'}}" class="bill-group-head">...</view>
     <view wx:elif="{{item._type === 'bill'}}" class="bill-item">...</view>
   </block>
   ```
   避免嵌套 `wx:for`，渲染层只遍历一次。
3. **`hidden` vs `wx:if`**：长列表里频繁切换的弹层/筛选条用 `hidden`（`display:none`），不要用 `wx:if`（节点反复创建销毁）。
4. **`<image>` 一定加 `mode`**，并对重复 logo 用 `display:inline-block; content` 字符（如果合适）。

**验收**：
- 100 条账单列表滑动的 FPS 在开发者工具"性能"面板 ≥ 50。

---

## Step 12：setData 合并与按需刷新 🟢

**现象**：账单保存后首页重新渲染时，整页闪一下。
**根因**：
1. `loadHomeData` 把 4 个概览数字、10 条账单、组结构等分别 `setData`。
2. `onShow` 和 `onPullDownRefresh` 都跑同一份逻辑，每次都整体重 setData。
**改法**：
1. **合并 setData**：一次 `setData` 把所有字段传完。
2. **增量更新**：保存一笔后，只把这一条用 `unshift` 进 `recentBills`，只更新 `recentBills` 和对应 group 的 `expense/income`。
3. **下拉刷新完成后，对比新数据与现有 data**：相同则不 setData，避免无谓渲染。
4. **`onShow` 触发时**，先检查 `wx.getStorageSync("home:summary:ts")` 是否 < 5s，是则跳过重拉。

**验收**：
- 开发者工具"性能"面板录制对比：保存一笔后首页渲染耗时下降 50%。

---

## Step 13：上线前的验收清单

执行完上述步骤后，逐条验证：

- [ ] 冷启动 → 首页可见内容 < 600ms（Vivo/小米中端机实测）。
- [ ] 首页 `getHomeSummary` 云函数 `durationMs` < 300ms（云开发控制台）。
- [ ] 账单页同月内切进切出 < 200ms（命中本地缓存）。
- [ ] 切换月份、切筛选项有 200ms 防抖。
- [ ] "保存"按钮按下到页面返回 < 800ms。
- [ ] 统计页 ECharts 体积优化后，主包大小下降 50KB+。
- [ ] 切账本后所有页面无残留旧账本数据。
- [ ] WXML 列表滑动 FPS ≥ 50（100 条账单）。
- [ ] 真机弱网（3G/4G 弱信号）下无白屏时间 > 1.5s 的卡顿。
- [ ] 微信开发者工具"性能 Trace" 面板无 `Long Task > 200ms` 警告。

---

## 附：改动文件清单（按步骤聚合）

| 步骤 | 主要改动文件 |
| --- | --- |
| 1 | `cloudfunctions/accountingFunctions/index.js`（加缓存） |
| 2 | `cloudfunctions/accountingFunctions/index.js`（`getStats` 改 aggregate + 移除多余查） |
| 3 | `cloudfunctions/accountingFunctions/index.js`（精简返回字段） |
| 4 | `cloudfunctions/accountingFunctions/index.js`（新增 `getHomeSummary`）<br>`miniprogram/pages/index/index.js`（替换调用） |
| 5 | `miniprogram/app.js`、`miniprogram/pages/index/index.{js,wxml,wxss}` |
| 6 | `miniprogram/pages/addBill/index.js`、`cloudfunctions/accountingFunctions/index.js` |
| 7 | `miniprogram/pages/bills/index.js`、`miniprogram/pages/stats/index.js` |
| 8 | `miniprogram/pages/stats/index.js`、`miniprogram/ec-canvas/*` |
| 9 | `miniprogram/app.js`、各页面 `onShow` 改造 |
| 10 | `miniprogram/images/brand/*`、`miniprogram/images/icons/*` |
| 11 | `miniprogram/pages/index/index.{js,wxml}`、`miniprogram/pages/bills/index.{js,wxml}` |
| 12 | `miniprogram/pages/index/index.js`、`miniprogram/pages/bills/index.js` |
| 13 | 真机测试 + 微信开发者工具"性能 Trace" |

---

> 建议执行节奏：
> 1. **第 1 周**完成 Step 1、2、3、4、5（云函数瘦身 + 首页优化），这一波能看到首屏/保存明显变快。
> 2. **第 2 周**完成 Step 6、7、9（保存、列表缓存、账本切换）。
> 3. **第 3 周**完成 Step 8、10、11、12（包体积、渲染层打磨）。
> 4. **第 4 周**做 Step 13 验收、补测试用例。
