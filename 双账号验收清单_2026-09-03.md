# 双账号 18 场景验收记录（目标 5 · 2026-09-03）

- 角色：A = 管理员（开发者工具/账号 A）；B = 普通成员（真机/账号 B）
- 环境：cloud1-d1gq4g6a7c2911b56（已部署 ledger/accounting/feedback/resetTestData）
- 门禁：`npm test` 115 项 0 失败；`verify` 13 步 0 告警
- 状态：⬜ 待执行 / ✅ 通过 / ❌ 失败（附现象）/ ⚠️ 阻塞

| # | 场景 | 执行说明 | 状态 |
|---|---|---|---|
| 1 | 初始化 | A 首次登录：默认账本+分类+账户+7 天邀请码各 1 份；重复进入不重复创建 | ⬜ |
| 2 | 邀请加入 | B 输入 A 的邀请码：先见账本名/管理员/成员数；确认前无成员关系；确认后 B 成为普通成员并切到该账本 | ⬜ |
| 3 | 权限 | B 只能编辑/删除自己的账单；A 可操作全部；伪造 familyId 被云函数拒绝 | ⬜ |
| 4 | 管理员转让 | A 不能转给自己；仅可转给有效普通成员；转让后唯一管理员；原管理员变成员 | ⬜ |
| 5 | 账本切换 | A/B 各建第二个账本后切换：首页/账单/统计/预算/分类/账户都只读新账本内容；失败切换不改变缓存 | ⬜ |
| 6 | 账单 | 金额/分类/账户/日期/成员云端校验；编辑/删除版本冲突；删除后统计与预算立即排除 | ⬜ |
| 7 | 分类账户 | 未使用物理删除；已使用停用；最后一个可用账户/一级分类下最后一个二级分类不可删/停用 | ⬜ |
| 8 | 预算 | 无预算、80% 预警、100% 超支、删除、未来月份，显示均正确 | ⬜ |
| 9 | 导入导出 | 预览展示成员映射；唯一昵称自动匹配、同名归入管理员；重复账单跳过；撤销只影响该批次；导出全部/筛选可用 Excel 打开 | ⬜ |
| 10 | 时区 | 上海跨日/月末/未来账单：首页与预算默认月不含未来；显式范围统计含未来 | ⬜ |
| 11 | 账号注销 | B 注销：退出全部账本、资料/偏好清理；A 有其他成员时先转让；仅剩自己时先解散；历史/搜索/导出显示“已注销用户” | ⬜ |
| 12 | 解散与恢复 | 解散后：旧缓存、伪造 familyId、旧邀请码均不可读写/加入；注销中断重试只补剩余步骤 | ⬜ |
| 13 | 邀请信息最小暴露 | 邀请确认只返回账本名/管理员昵称/成员数/状态；分享路径不带 familyId | ⬜ |
| 14 | 解散中断恢复 | 成员状态更新成功、账本状态更新失败的中断后，重试解散收敛到 families.status=dissolved，成员不可访问，邀请码撤销，family.dissolve 审计不重复 | ⬜ |
| 15 | 成员并发加入 | A 与 B 同时确认同一邀请码均成功；第 51 人拒绝；同一用户重复确认幂等 | ⬜ |
| 16 | 遗留成员收敛 | 随机 ID 遗留成员文档用户再次加入收敛为单条，无重复 active | ⬜ |
| 17 | 账单并发编辑 | A/B 同一账单同时编辑：唯一请求成功返回新版本；另一个版本冲突被拒并提示刷新 | ⬜ |
| 18 | 管理员转让并发 | 管理员同时两次转让给不同成员：仅一次成功，账本始终一名管理员 | ⬜ |

## 备注
- 涉及真机的项（2/5/10/11）与真机走查合并执行。
- 需要 B 手工制造并发（15/17/18）时，用微信双开/双设备或快速连点配合。
