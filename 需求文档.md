# 家庭记账本小程序 — 需求文档

## 1. 产品概述

一款基于微信小程序的家庭共享记账应用，支持多人通过邀请码/二维码加入同一家庭账本，协同记录日常收支，提供统计图表、预算管理、数据导入导出等功能。

- **目标用户**：家庭成员（2~N 人），共同记录家庭流水
- **技术方案**：微信小程序 + 微信云开发（云数据库 + 云函数）
- **币种**：仅支持人民币（CNY）

---

## 2. 用户体系与权限

### 2.1 登录方式

- 微信授权登录（`wx.login` + 云开发获取 openid）
- 无需额外注册，授权即用

### 2.2 家庭账本

- 用户可创建多个家庭账本，也可通过邀请码/二维码加入他人的账本
- 一个用户可同时属于多个家庭账本，支持随时切换查看
- 每个家庭账本有一个创建者，即为该账本的**管理员**
- 管理员可将管理员身份转让给账本内另一位成员；转让后原管理员降为普通成员
- 管理员必须先转让管理员身份，才能退出账本

### 2.3 成员

- 加入家庭账本的用户即为该账本的成员
- 无需单独添加不登录的成员，账本成员均为已登录微信用户
- 账单的**归属成员**表示这笔收支属于谁，可选择当前账本内任一成员
- 账单不再保留独立的"记账人"字段，成员字段同时表示归属成员
- 系统内部记录原始创建者用于权限校验和审计，不在表单中展示
- 成员退出账本后，其历史记录保留；账单中保留归属成员的昵称、头像快照

### 2.4 权限模型

| 操作 | 普通成员 | 管理员 |
|------|---------|--------|
| 记账（新增） | ✅ | ✅ |
| 编辑自己记的账 | ✅ | ✅ |
| 删除自己记的账 | ✅ | ✅ |
| 编辑他人记的账 | ❌ | ✅ |
| 删除他人记的账 | ❌ | ✅ |
| 邀请/移除成员 | ❌ | ✅ |
| 设置预算 | ❌ | ✅ |
| 管理分类 | ❌ | ✅ |
| 管理员转让 | ❌ | ✅ |

---

## 3. 记账功能

### 3.1 记账入口

- 首页「记一笔」按钮，点击进入独立记账页面
- 填完即存，步骤尽量少
- **不支持**一笔账按多分类拆分

### 3.2 账单字段

每笔账单记录以下信息：

| 字段 | 说明 | 是否必填 |
|------|------|---------|
| 交易类型 | 支出 / 收入 | ✅ |
| 金额 | 数值 | ✅ |
| 一级分类 | 用户自定义 | ✅ |
| 二级分类 | 用户自定义 | ✅ |
| 日期 | 默认当前时间，可修改 | ✅ |
| 账户 | 标签（如现金、银行卡、微信），仅用于筛选，不管理余额 | ✅ |
| 成员 | 谁花的钱/谁收的钱，默认当前用户，可选择其他成员 | ✅ |
| 备注 | 文字描述 | 选填 |
| 商家 | 可选 | 选填 |

> 不支持：图片凭证、标签、定位、转账、多币种

**金额与时间规则：**

- 金额必须大于 0，最多保留两位小数
- 金额以分为单位保存为整数，页面展示时转换为元，避免浮点计算误差
- 日期按中国标准时间（Asia/Shanghai）处理；月度统计按账单日期归属月份计算
- 结余 = 收入 - 支出

**删除规则：**

- 账单使用软删除，删除后默认列表、统计、预算均不再包含该账单
- 保留 `deletedAt`、`deletedBy` 和删除前账单快照，管理员可在后续版本恢复或审计

### 3.3 分类管理

- 一级分类和二级分类均由用户完全自由增删改
- 一级分类和二级分类均有对应的 Emoji 图标
- 新建家庭账本时预设一套默认分类，用户可在此基础上调整
- 删除分类只影响后续记账，不修改历史账单；账单保存分类名称和图标快照
- 删除含有二级分类的一级分类前，需明确处理其仍可用于新账的二级分类

**默认支出分类（一级分类 🍜 → 二级分类）：**

| 一级分类 | 图标 | 二级分类（图标） |
|---------|------|----------------|
| 食品酒水 | 🍜 | 早餐 🥣、午餐 🍱、晚餐 🍲、夜宵 🍢、水果蔬菜 🥬、零食 🍫、饮料 🧃、酒水 🍺、超市购物 🛒、外卖 🛵 |
| 交流通讯 | 📱 | 话费 📞、上网费 📶、邮寄快递 📦 |
| 居家物业 | 🏠 | 房租房贷 🏠、水电煤气 💡、物业管理 🧹、日常用品 🧴、家具家电 🛋️ |
| 行车交通 | 🚗 | 打车租车 🚕、公交地铁 🚇、加油 ⛽、停车费 🅿️、维修保养 🔧、高速过路 🛣️、机票火车 🚄 |
| 休闲娱乐 | 🎮 | 电影演出 🎬、咖啡奶茶 ☕、运动健身 🏃、旅游度假 🏖️、聚餐聚会 🍻、游戏 🎮、KTV 🎤 |
| 人情交际 | 🎁 | 礼物 🎁、红包份子 🧧、请客 🍽️ |
| 医疗健康 | 💊 | 门诊挂号 🏥、药品 💊、体检 🩺 |
| 服饰美容 | 👗 | 衣服鞋帽 👕、护肤化妆 💄、美发美容 💇 |
| 进修学习 | 📚 | 书籍 📖、培训课程 🎓、学习用品 ✏️ |
| 数码电器 | 💻 | 手机电脑 💻、数码配件 🔌 |
| 母婴亲子 | 👶 | 奶粉尿裤 🍼、玩具 🧸、早教 🎨 |
| 宠物 | 🐶 | 宠物食品 🦴、宠物医疗 🐾 |
| 金融保险 | 🏦 | 保险费 🛡️、银行手续费 🏧 |
| 其他支出 | ❓ | 其他 ❓ |

**默认收入分类（一级分类 💼 → 二级分类）：**

| 一级分类 | 图标 | 二级分类（图标） |
|---------|------|----------------|
| 职业收入 | 💼 | 工资 💰、奖金 🎉、兼职收入 💪 |
| 投资理财 | 📈 | 利息 🏦、股票基金 📊、理财收益 💵 |
| 礼金收入 | 🎁 | 红包 🧧、礼金 💝 |
| 其他收入 | ❓ | 其他 ❓ |

---

## 4. 页面结构

小程序包含 4 个 Tab 页 + 若干子页面：

### 4.1 首页（记账）

- 顶部：当前家庭账本名称，支持切换账本
- 概览区：今日支出、本月支出、本月收入、本月结余
- 「记一笔」大按钮
- 最近账单列表（最近 10~20 条）

### 4.2 统计页

- 月度收支汇总卡片（支出、收入、结余）
- 饼图：支出按一级分类占比
- 折线图：每日支出趋势
- 支持切换月份
- 支持切换收支类型查看

### 4.3 账单页

- 全部流水列表，按时间倒序
- 筛选：按时间范围、分类、成员、账户
- **搜索**：按备注、商家、金额关键词搜索
- 支持编辑/删除（根据权限）

### 4.4 我的页

- 用户头像与昵称
- 家庭管理：创建账本、切换账本、邀请成员（生成邀请码/二维码）、成员列表、移除成员
- 预算设置：设置每月总支出预算
- 分类管理：增删改一二级分类
- 账户管理：增删改账户标签
- 数据导入：上传 xlsx 文件导入账单
- 数据导出：导出账单为 xlsx/csv
- 记账提醒设置
- 关于

---

## 5. 统计报表

- 月度收支汇总：本月总支出、总收入、结余
- 饼图：支出按一级分类占比分布
- 折线图：每日支出金额趋势
- 支持按月切换查看
- 支持按支出/收入类型切换
- 所有统计仅包含未删除账单，并随新增、编辑、删除账单实时更新

---

## 6. 预算管理

- 每月设置一个总支出预算金额（管理员设置）
- 实时统计本月已支出 vs 预算
- 当预算使用达到 80% 时显示预警；达到或超过 100% 时显示超支提示
- 超支时在小程序内提醒（首页/统计页显示超支提示）
- 不支持按分类设预算

---

## 7. 数据导入与导出

### 7.1 导入

- 支持导入「随手记」App 导出的 xlsx 格式文件
- 文件结构：两个 Sheet（支出、收入），字段如下：

| 字段 | 说明 |
|------|------|
| 交易类型 | 支出 / 收入 |
| 日期 | 如 `2026-07-31 15:55:13` |
| 一级分类 | 如 `食品酒水` |
| 二级分类 | 如 `水果蔬菜肉` |
| 支出账户/收入账户 | 如 `现金` |
| 账户币种 | 固定 `CNY`（导入时忽略） |
| 金额 | 数值 |
| 成员 | 如 `坤坤`（导入时映射为账本成员） |
| 商家 | 可空 |
| 项目分类 | 可空（导入时忽略） |
| 项目 | 可空（导入时忽略） |
| 备注 | 如 `朴朴买菜` |

- 导入时自动创建不存在的分类和账户
- 导入前展示成员映射：导出文件中的成员字段匹配当前账本成员；未映射的统一归为导入操作人
- 导入账单的实际创建人统一为当前导入操作人
- 同一文件重复导入时，根据交易类型、日期、金额、分类、备注生成指纹并提示重复条目；用户确认后跳过重复项
- 导入前预览，确认后再写入
- 文件格式、必填列、日期和金额不合法时，展示具体行号和原因，不写入任何数据

### 7.2 导出

- 支持导出当前账单为 xlsx 和 csv 格式
- 导出字段与账单字段一致
- 支持按时间范围筛选导出

---

## 8. 搜索功能

- 在账单页提供搜索入口
- 支持按备注、商家关键词模糊搜索（忽略大小写）
- 支持按金额精确搜索，金额最多两位小数
- 空关键词时不执行搜索；无结果时展示空状态
- 搜索结果在账单列表中展示，可编辑/删除（受权限控制）

---

## 9. 记账提醒

- 用户可开启/关闭提醒
- 定时提醒：每日固定时间提醒记账（如每晚 21:00）
- 智能提醒：检测到当天未记账时额外提醒
- 通过微信小程序订阅消息实现，用户须主动授权订阅
- 若用户未授权、拒绝订阅或订阅次数耗尽，仅在首页和统计页展示页面内提醒，不承诺后台推送

---

## 10. 数据存储（云开发）

### 10.1 云数据库集合设计

| 集合名 | 说明 |
|--------|------|
| `users` | 用户信息（openid、昵称、头像、创建时间、更新时间） |
| `families` | 家庭账本（名称、创建者openid、创建时间、更新时间） |
| `family_members` | 账本成员关系（familyId、openid、昵称快照、头像快照、角色、加入/退出时间） |
| `family_invites` | 家庭邀请码（familyId、code、状态、创建人、创建时间、过期时间） |
| `bills` | 账单（familyId、类型、金额分、分类快照、账户快照、归属成员快照、创建者openid、日期、备注、商家、创建/更新时间、版本号、软删除字段、导入指纹） |
| `categories` | 分类（familyId、一级分类、一级图标、二级分类、二级图标、类型、是否启用、创建/更新时间） |
| `accounts` | 账户标签（familyId、名称、是否启用、创建/更新时间） |
| `budgets` | 预算（familyId、月份、金额分、创建/更新时间） |

### 10.2 云函数

| 函数名 | 说明 |
|--------|------|
| 登录/用户初始化 | 获取 openid，创建/更新用户信息 |
| 家庭账本管理 | 创建、查询、切换账本，生成邀请码 |
| 成员管理 | 加入、退出、移除成员 |
| 账单 CRUD | 新增、编辑、删除、查询账单（含权限校验） |
| 分类管理 | 增删改查分类 |
| 账户管理 | 增删改查账户标签 |
| 统计聚合 | 月度汇总、分类占比、趋势数据 |
| 预算管理 | 设置、查询预算 |
| 数据导入 | 解析 xlsx，写入账单 |
| 数据导出 | 查询账单，生成 xlsx/csv |

### 10.3 数据安全

- 所有写操作通过云函数执行，在云函数内校验权限
- 普通成员只能编辑/删除自己创建的账单
- 管理员可操作所有账单及账本设置
- 数据库集合设置最小权限，读写均通过云函数
- 账单查询必须按 `familyId` 和未删除状态过滤，并实现分页，避免一次读取全部账单
- 为账单建立 `familyId + date`、`familyId + deletedAt` 等查询索引；导入指纹建立唯一性校验逻辑

---

## 11. MVP 验收标准

首个可用版本至少满足以下条件：

- 用户可以创建家庭账本，并通过邀请码或二维码加入家庭
- 管理员可以邀请、移除成员和转让管理员身份；普通成员不能执行管理员操作
- 用户可以新增收入或支出，并填写金额、分类、日期、账户、归属成员和备注
- 普通成员只能编辑、删除自己创建的账单；管理员可以操作全部账单
- 首页、账单页和统计页只展示当前家庭且未删除的账单
- 月度支出、收入、结余、分类占比和每日趋势数据计算一致
- 管理员可以设置月度总预算，达到 80% 和 100% 时显示对应提示
- 随手记 xlsx 导入支持预览、字段校验、成员映射和重复提示
- 账单可按时间、分类、成员、账户筛选，并可搜索备注、商家和金额
- 导出文件可被 Excel 正常打开，金额、日期、分类和备注不丢失

## 12. 异常与空状态

- 未创建或未加入家庭时，引导创建家庭或输入邀请码
- 无账单、无搜索结果、无预算、无成员时展示明确空状态和可执行操作
- 云函数失败、网络中断、权限不足时展示错误提示，并避免重复提交
- 保存账单前校验金额、分类、账户、归属成员和日期等必填项
- 删除账单前二次确认，删除成功后立即刷新首页、账单和统计数据
- 导入失败时展示文件级或行级错误；批量导入采用整批校验通过后再写入
- 账本切换后清空旧账本页面缓存，防止不同家庭数据混用

---

## 13. 不在当前版本范围内的功能

以下功能本次不做，后续可迭代：

- 多币种与汇率换算
- 账户余额管理 / 账户间转账
- 图片凭证（小票/发票拍照）
- 账单标签 / 定位
- 一笔账多分类拆分
- 分类预算
- 年度账单总结
- 账单截图分享
- 同比环比深度分析
- 自定义时间区间统计
- 成员维度对比分析

---

## 14. 技术依赖

- 微信小程序原生开发框架
- 微信云开发（云函数 Node.js + 云数据库）
- 图表：`ec-canvas`（ECharts 微信小程序版）
- xlsx 解析：云函数中使用 `xlsx` 库

---

## 已确认需求基线

本文档的后续补充和已经确认的业务规则统一记录于项目根目录 **`需求基线.md`**，自 2026-08-10 起生效。

开发和产品决策发生冲突时，优先级为：
1. 用户最新明确确认
2. `需求基线.md`
3. 本文档
4. 现有代码实现

当前版本暂缓功能（图片凭证、周期性记账、账单标签、收藏置顶等）以 `需求基线.md` 的"当前版本暂缓"章节为准，未经再次确认不得提前实现。

---

*文档版本：v1.2  |  更新日期：2026-08-10  |  新增需求基线引用*
