# V1.2 候选功能清单

> 本文档统一收纳 V1.1 收尾后所有"暂缓但可能做"的功能。**任何 V1.2 启动前需用户明确确认**，不得在 V1.1 偷偷启用。
> 创建日期：2026-08-19
> 前置：V1.1 发布完成

---

## 优先级评估框架

| 维度 | 说明 |
|---|---|
| 业务价值 | 解决多少用户的痛点 / 增加多少活跃度 |
| 实现成本 | 开发工时（人天）+ 风险 |
| 主包影响 | 是否会让主包逼近 2MB 门槛 |
| 数据规模 | 当前数据量下性能是否撑得住 |

进入 V1.2 必须满足"业务价值 ≥ 实现成本"或"用户明确要求"。

---

## 候选清单

### 1. 导入性能 + 进度条（几千条账单场景）

**背景**：当前 `confirmImport` 是同步处理，每行账单 4 次串行 DB 查询 + 1 次插入 ≈ 50-100ms/行。

| 场景 | 耗时估算 | 当前是否撑得住 |
|---|---|---|
| 100 条 | 5-10 秒 | ✅（实测） |
| 500 条 | 25-50 秒 | ⚠️ 接近 60 秒 config 超时 |
| 1000 条 | 50-100 秒 | ❌ 必爆超时 |
| 3000+ 条 | 150-300 秒 | ❌ 完全不可用 |

**候选方案**

- **方案 A：分批 + 进度条（推荐）**
  - 客户端按 200 条/批拆分，连续调 `confirmImport` 多次
  - 每次云函数返回时客户端更新进度条（`已导入 N / 总 M 条`）
  - 任一批失败可回滚该批 + 中断后续
  - 工作量：~1 人天
  - 风险：低（兼容现有 `confirmImport` / `rollbackImport`）

- **方案 B：异步任务 + 推送**
  - 客户端发起 `startImport` 后立即返回
  - 云函数后台跑，写入进度到 `import_tasks` 集合
  - 客户端 `db.watch` 监听该任务进度
  - 工作量：~2 人天
  - 风险：中（新增集合 + 实时监听）

- **方案 C：服务端性能优化**
  - `confirmImport` 改为 `bulkWrite` / 批量 add
  - 缓存常用分类/账户查询结果
  - 工作量：~2-3 人天
  - 风险：高（事务一致性需要重新设计）

**进入条件**：用户导入账本量 ≥ 500 条

---

### 2. ECharts 1MB 替换（来自 [ECHARTS_OPTIMIZATION.md](./ECHARTS_OPTIMIZATION.md)）

- 候选方案：uCharts 0.3MB / 自绘 Canvas / 按需加载 ECharts 子模块
- 当前余量：116KB（主包 1884KB < 2MB）
- 进入条件：主包 ≥ 1.9MB 或新需求加图表相关代码

---

### 3. 排序 UI 入口

- 服务端 `listBills` 已支持 `amountAsc/amountDesc`，仅缺 UI
- 工作量：~0.5 人天
- 风险：低
- 进入条件：用户明确提出要排序

---

### 4. 通知类

- 每日记账提醒（订阅消息）
- 预算提醒（订阅消息）
- 成员记账通知（应用内）
- 管理员修改他人账单通知（应用内）
- 进入条件：用户明确要求 + 订阅消息合规审核

---

### 5. 账单图片凭证

- 需求基线 §17 暂缓
- 工作量：~3-5 人天（云存储 + 缩略图 + 防刷 + 隐私）
- 风险：中（云存储费用 + 上传/下载体验）

---

### 6. 周期/重复自动记账

- 需求基线 §17 暂缓
- 工作量：~2 人天
- 风险：中（边界场景多：节假日跳过 / 月末对齐 / 取消规则）

---

### 7. 账单自由标签

- 需求基线 §17 暂缓
- 工作量：~1 人天
- 风险：低

---

### 8. 分类预算

- 需求文档 §13 不在 MVP
- 工作量：~1 人天（基于现有月预算结构扩展）
- 风险：低

---

### 9. 年度账单总结 / 同比环比

- 需求文档 §13 不在 MVP
- 工作量：~2-3 人天
- 风险：中（数据量大时的聚合性能）

---

### 10. 账单收藏/置顶

- 需求基线 §17 暂缓
- 进入条件：用户明确提出

---

## 进入 V1.2 流程

1. 用户从候选清单中圈定本次要做的功能
2. 评估每个功能的"业务价值 vs 实现成本"
3. 创建 `V1.2_发布计划与验收目标.md` 文档（仿 V1.1 模板）
4. 按 Phase 7（代码清理）→ 8/9/10（外部验收）→ 11（发版）的流程执行
5. V1.1 的"暂缓"决策**不能**在 V1.2 自动启用——每个功能都要单独评估

---

*V1.2 候选 · 2026-08-19 收 V1.1 末位清单*
