# 家庭记账本 V1.1 发布计划与验收目标

> 文档状态：待执行
> 创建日期：2026-08-18
> 前置：V1.0（2026-08-14 收尾的 MVP 计划）代码层面全部完成
> 维护规则：每完成一个阶段，更新「进度记录、风险与验收结果」；任何未达标项不得标记为完成
> 关联文档：
> - V1.0 计划：`家庭记账本_MVP开发计划与高标准验收目标.md`
> - 需求：`需求基线.md` / `需求文档.md`
> - 执行清单：`云开发部署与验收清单.md`（V1.0）/ [PHASE_8_DEPLOY.md](./PHASE_8_DEPLOY.md) / [PHASE_9_E2E.md](./PHASE_9_E2E.md) / [PHASE_10_DEVICE.md](./PHASE_10_DEVICE.md) / [PHASE_11_RELEASE.md](./PHASE_11_RELEASE.md)
> - V1.2 候选：[V1.2_CANDIDATES.md](./V1.2_CANDIDATES.md) / [ECHARTS_OPTIMIZATION.md](./ECHARTS_OPTIMIZATION.md)

---

## 1. V1.1 目标

在 V1.0 代码已完整的基础上，**完成云端部署、真实数据联调与真机视觉走查**，交付一个**可在生产环境发布**的家庭记账本。

**成功标志**（全部满足才算 V1.1 完成）：
- `npm run verify` 12 步全绿
- `npm test` 60+ 项全绿
- 主包体积 < 2MB
- 云端 11 个集合 + 13 条索引就位
- 2 个测试账号完成 18 个联调场景
- 真实随手记 xlsx 完成 预览 → 确认 → 撤销 全流程
- 真机走查无 P0/P1 问题（iPhone 刘海屏 + 小屏 Android + 键盘顶起 + 长文本）
- ECharts Canvas 在真机渲染、触控正常

**V1.1 不做的事**（明确暂缓到 V1.1 之后）：
- 排序 UI 入口（基线 §12 第 8 条延期）
- 通知类（每日提醒/预算提醒/成员记账通知）
- 账单图片凭证、周期记账、账单标签、收藏置顶
- 多币种、账户余额、一笔账多分类拆分

---

## 2. V1.0 → V1.1 范围对比

| 维度 | V1.0（MVP 计划 Phase 0-6） | V1.1（发布计划） |
|---|---|---|
| 代码完整性 | 全部完成 ✅ | 修 2 个 pre-existing 缺口 |
| 静态验收 | 通过 11/12 步（主包 2118KB 略超） | 修复到 12/12 通过 |
| 单元+契约测试 | 60 项，1 项失败（`getStats 当前月默认排除未来账单`） | 修复到 60+/60+ 通过 |
| 云端部署 | 未执行 | 完成云函数 + 集合 + 索引 |
| 真机联调 | 未执行 | 18 个场景 + 真实 xlsx 端到端 |
| 真机视觉 | 未执行 | iPhone/小屏 Android/键盘/长文本 |
| 通知/排序 UI/图片凭证等 | 全部暂缓 | 仍暂缓（写入 V1.1 暂缓清单） |

---

## 3. V1.1 阶段划分

### Phase 7：发布前代码清理

**目标**：把 `npm run verify` 和 `npm test` 全绿，扫清所有发布阻塞。

**任务清单**：
- [ ] 7.1 修复主包体积 2118KB 超 2MB 上限
  - 主包 1.0MB 来自 `ec-canvas`（ECharts 库）+ 896KB 品牌资源
  - 优先压品牌资源：插画 400px → 350px、Logo 600px → 480px
  - 仍超则剔除最少使用的资源（评估：搜索无结果插画是否常驻）
  - 仍超则考虑 ECharts 按需引入或换更轻的 chart 库
- [ ] 7.2 修复 `getStats 当前月默认排除未来账单` 契约测试失败
  - 根因：`scripts/fake-db.js` 缺 `db.collection().aggregate().match().group().end()` 支持
  - 实现：补 `aggregate()` 方法 + `match()` / `group()` 链式 + 内存聚合
  - 验收：该测试与 60 项其它测试全部通过
- [ ] 7.3 复跑 `npm run verify` 和 `npm test`，确认 12 步 + 60+ 项全绿

**交付物**：
- `npm run verify` 12/12 通过
- `npm test` 60+/60+ 通过
- 主包 < 2MB

---

### Phase 8：云端部署

**目标**：把代码部署到云开发环境并初始化数据库。

**任务清单**：
- [x] 8.0 部署前预检（环境 ID / 依赖 / 死代码清理）—— 已完成
- [ ] 8.1 在云开发控制台创建 11 个集合，设权限"仅创建者可读写"（需用户手动）
  - 详见 `PHASE_8_DEPLOY.md` §1
- [ ] 8.2 按 `PHASE_8_DEPLOY.md` §2 配置 13 条索引（需用户手动）
- [x] 8.3 在 `accountingFunctions/` 目录执行 `npm install`（xlsx 0.18.5 已装）
- [ ] 8.4 上传并部署云函数（可用 `./uploadCloudFunction.sh` 自动执行）
  - 先 `ledgerFunctions`，再 `accountingFunctions`
- [ ] 8.5 清除 2 个测试账号的旧数据（可调用 `resetTestData` 云函数）
- [ ] 8.6 上传小程序代码到开发者工具
- [x] 8.7 验证 `app.js` 中 `globalData.env` 与云环境 ID 一致
  - `app.js` env = `cloud1-d1gq4g6a7c2911b56`，与 `envList.js` 一致 ✅

**交付物**：
- 11 个集合 + 13 条索引就位
- 2 个云函数部署成功
- 小程序代码上传成功

---

### Phase 9：真实数据联调

> 📋 **详细场景清单**：[`PHASE_9_E2E.md`](./PHASE_9_E2E.md) —— 18 场景（初始化 / 邀请 / 权限 / 导入 / 统计 / 注销 / 解散 / 并发）
> 🧪 **测试 xlsx 生成**：`node scripts/gen-test-xlsx.js /tmp/test-suijiji.xlsx`

**目标**：用 2 个真实微信账号在云端验证全部业务规则。

**任务清单**：
- [ ] 9.1 按 `云开发部署与验收清单.md` §3 执行 18 个场景
  - 初始化 / 邀请加入 / 权限 / 管理员转让 / 账本切换 / 账单 / 分类账户 / 预算 / 导入导出 / 时区 / 账号注销 / 解散与恢复 / 邀请信息最小暴露 / 解散中断恢复 / 成员并发加入 / 遗留成员收敛 / 账单并发编辑 / 管理员转让并发
- [ ] 9.2 真实随手记 xlsx 端到端
  - 选 1 个真实导出文件（支出 + 收入 2 个 Sheet）
  - 跑 选文件 → 预览 → 成员映射 → 确认导入 → 撤销
  - 验证批次写入、批次撤销、自动创建分类/账户、唯一昵称匹配、重复检测
- [ ] 9.3 修复 Phase 9 暴露的所有问题
  - 事务 SDK 行为差异
  - 并发冲突未覆盖
  - 真实 xlsx 字段格式与 mock 差异
  - 权限边界漏洞
- [ ] 9.4 给修复的问题补契约测试

**交付物**：
- 18 个场景全部通过
- 真实 xlsx 端到端通过
- `npm test` 仍 60+/60+ 通过（补的测试也通过）

---

### Phase 10：真机视觉与图表

> 📋 **详细走查清单**：[`PHASE_10_DEVICE.md`](./PHASE_10_DEVICE.md) —— 3 设备（iPhone / 小屏 Android / 大屏 Android）+ 8 类场景（导航 / Tab / FAB / 弹层 / 键盘 / 长文本 / 状态 / ECharts）

**目标**：在真机上完成视觉与图表的最后验证。

**任务清单**：
- [ ] 10.1 iPhone 刘海屏走查（iPhone 14/15/16）
  - 自定义导航是否避开刘海
  - 底部 TabBar/FAB 不被 Home Indicator 遮挡
  - 底部弹层（账本切换/筛选）不被遮挡
- [ ] 10.2 小屏 Android 走查（360px 宽）
  - 概览卡 2×2 不挤压
  - 账单行内容完整
  - 预算页输入框与按钮可用
- [ ] 10.3 大屏 Android 走查（412px+ 宽）
  - 布局合理利用空间
  - 弹层居中
- [ ] 10.4 键盘弹起
  - 记账表单：金额输入不顶起破坏布局
  - 筛选页：备注/商家输入不顶起破坏布局
  - 修改昵称弹窗：输入框不被遮挡
- [ ] 10.5 长文本
  - 长账本名：列表/弹层不撑破
  - 长昵称：成员列表/账单行省略号
  - 长备注/商家：账单行省略号
  - 大金额（999,999.99）：不撑破列宽
- [ ] 10.6 状态走查
  - 空态：首页/账单/搜索/统计
  - 错误态：网络异常/权限拒绝
  - 加载态：首屏/分页/统计
  - 弱网恢复：杀掉重启
- [ ] 10.7 ECharts Canvas 真机验证
  - 饼图初始化与渲染
  - 折线图初始化与渲染
  - 图表触控（点击查看分类明细）
  - 切换月份/年份时图表刷新

**交付物**：
- 真机走查报告（含截图）
- 所有 P0/P1 问题已修复

---

### Phase 11：发版候选

**目标**：交付一个可发布的版本。

**任务清单**：
- [ ] 11.1 复跑 `npm run verify`（最终确认 12/12）
- [ ] 11.2 复跑 `npm test`（最终确认 60+/60+）
- [ ] 11.3 复测主包体积（最终 < 2MB）
- [ ] 11.4 更新 `README.md` 的"发布前清单"小节
- [ ] 11.5 在 `云开发部署与验收清单.md` 末尾追加最终验收结果
- [ ] 11.6 在 V1.0 MVP 计划文档追加"已发布至 V1.1"标记
- [ ] 11.7 走 [PHASE_11_RELEASE.md](./PHASE_11_RELEASE.md) 全流程：复跑 + Release Notes + 仓库侧 commit/tag + 体验版

**交付物**：
- 12/12 verify 通过
- 60+/60+ test 通过
- 主包 < 2MB
- 全部 18 个联调场景通过
- 全部真机走查 P0/P1 已修复
- 发版候选就绪

---

## 4. 高标准验收

### 4.1 功能完整性
- V1.0 所有功能在云端可用
- 18 个联调场景全过
- 真实 xlsx 端到端通过
- 通知/排序 UI/图片凭证等暂缓功能**不出现**在 V1.1

### 4.2 数据安全
- 前端不直连 DB（verify 步骤 2 通过）
- 全部写操作经云函数权限校验
- 双账号隔离 100% 正确
- 普通成员不能编辑/删除他人账单
- 跨账本访问被拒绝

### 4.3 视觉与体验
- 真机走查无 P0/P1 问题
- 长文本不溢出、不重叠、不撑破
- 键盘弹起不破坏布局
- 品牌 Logo 与插画在真机清晰、无裁切、无变形

### 4.4 性能（基线建议值）
- 首页加载 < 2s（4G）
- 账单分页 20 条 < 500ms
- 统计查询 < 1s
- 切换账本 < 1s

### 4.5 健壮性
- 弱网恢复不丢数据
- 操作中断不留垃圾（解散/导入）
- 重试不重复创建（初始化/邀请/账单）

### 4.6 静态门禁
- `npm run verify` 12/12 通过
- `npm test` 60+/60+ 通过
- 主包 < 2MB
- 7 张品牌资源 RGBA 透明
- 前端无 `wx.cloud.database` 直连
- 前端无 openid/creatorOpenId 等敏感字段残留

---

## 5. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 主包体积压不下来（2118KB） | 发布阻塞 | 优先压品牌资源；剔除最少使用；评估 ECharts 按需 |
| `aggregate()` 测试桩难实现 | 推迟发布 | 优先用 `getAllBills` 重写 getStats；或补 fake-db 桩 |
| 云端事务 SDK 行为与静态推理不一致 | 解散/邀请/管理员转让等事务可能失败 | Phase 9 优先跑这些场景，发现问题立即修 |
| 真实 xlsx 字段与 mock 不一致 | 导入报错 | Phase 9 用真实文件跑，发现差异立即修 |
| ECharts 真机性能差 | 统计页卡顿 | 改用更轻的 chart 库（如 wx-charts）或简化为柱状图 |
| 真实用户头像上传受限 | 用户无法上传头像 | 检查云存储权限；fallback 到默认头像 |
| 双账号联调发现问题多 | 推迟发布 | 按场景严重程度分级；P0/P1 必须修，P2/P3 进 V1.2 |

---

## 6. 不在 V1.1 范围（明确暂缓）

按 `需求基线.md` §17、§14 与本次确认的决策，以下功能**不在 V1.1 实施**：

| 功能 | 暂缓原因 | 计划版本 |
|---|---|---|
| 排序 UI 入口 | 业务价值低（默认 dateDesc 覆盖 90% 场景）；统计页已有排行 | V1.2+ 视用户反馈 |
| 每日记账提醒 | 需求基线 §14/§17 暂缓 | V1.2+ 视用户反馈 |
| 预算提醒订阅消息 | 需求基线 §14/§17 暂缓 | V1.2+ 视用户反馈 |
| 成员记账通知 | 需求基线 §14/§17 暂缓 | V1.2+ 视用户反馈 |
| 管理员修改他人账单通知 | 需求基线 §17 暂缓 | V1.2+ 视用户反馈 |
| 账单图片凭证 | 需求基线 §17 暂缓 | V1.2+ 视用户反馈 |
| 周期/重复自动记账 | 需求基线 §17 暂缓 | V1.3+ 评估 |
| 账单自由标签 | 需求基线 §17 暂缓 | V1.2+ 视用户反馈 |
| 账单收藏或置顶 | 需求基线 §17 暂缓 | V1.3+ 评估 |
| 多币种/汇率 | 需求文档 §13 不在 MVP | 暂不规划 |
| 账户余额/转账 | 需求文档 §13 不在 MVP | 暂不规划 |
| 一笔账多分类拆分 | 需求文档 §13 不在 MVP | 暂不规划 |
| 分类预算 | 需求文档 §13 不在 MVP | V1.2+ 评估 |
| 年度账单总结/同比环比 | 需求文档 §13 不在 MVP | V1.2+ 评估 |
| ECharts 1MB 替换为 uCharts 等轻量方案 | V1.1 余量 168KB 足够 | V1.2+ 评估（[ECHARTS_OPTIMIZATION.md](./ECHARTS_OPTIMIZATION.md) 方案 A） |
| 几千条账单导入 + 进度条 | V1.1 60s 超时仅能撑 500 条；1000+ 必爆 | V1.2+ 评估（[V1.2_CANDIDATES.md](./V1.2_CANDIDATES.md) 候选 1） |

---

## 7. 与既有文档的关系

- **V1.0 MVP 计划**（`家庭记账本_MVP开发计划与高标准验收目标.md`）：V1.0 的代码工作日志，本文档不替代。
- **需求基线**（`需求基线.md`）：业务规则来源；V1.1 不改基线条款。
- **需求文档**（`需求文档.md`）：产品需求来源；V1.1 不改产品需求。
- **云开发部署与验收清单**（`云开发部署与验收清单.md`）：V1.1 Phase 8/9 的执行清单。

**冲突优先级**（沿用既有规则）：
1. 用户最新明确确认
2. 需求基线
3. 需求文档
4. 现有代码实现
5. 本 V1.1 计划

---

## 8. 进度记录

### 2026-08-18 进度记录：Phase 7.1 主包体积压缩（品牌资源）

> 本次更新为本地静态与契约验证（不涉及云端部署/联调/真机），不标记 V1.1 完成。

#### 已完成

**Phase 7.1 品牌资源压缩（保持 RGBA + 缩小尺寸）**

- 备份原始 7 张品牌 PNG 到 `/tmp/brand-original/`，压缩后归档清理
- 用 `magick` 重新编码为 RGBA（colorType=6）+ 缩小尺寸 + 最高 PNG 压缩
  - bun-logo: 600×185 → 400×123（81KB → 55KB，-33%）
  - empty-bills: 400×324 → 300×243（111KB → 78KB，-30%）
  - import-done: 400×326 → 300×245（127KB → 93KB，-26%）
  - import-empty: 367×400 → 275×300（117KB → 81KB，-30%）
  - network-error: 400×399 → 300×299（114KB → 87KB，-23%）
  - search-empty: 400×398 → 300×299（160KB → 117KB，-27%）
  - welcome-guide: 400×300 → 300×225（126KB → 82KB，-35%）
- 全部 7 张经 ollama-vision 视觉抽检（Logo / 空账单 / 欢迎引导 / 搜索无结果）：
  文字清晰、包子形象完整、颜色与原图一致、肉眼无明显质量损失

**ECharts 1MB 暂未处理**

- `miniprogram/ec-canvas/echarts.js` 仍是 1006KB / 54% 主包
- 暂不动：ECharts 是预编译 UMD bundle（不能 tree-shake），且当前仅 stats 页用饼图+折线图
- 后续若需优化：考虑按需加载（云存储下载）或换轻量库（wx-charts / uCharts）

#### 验证结果

| 检查项 | 结果 |
|---|---|
| `npm run verify` 步骤 9（主包 < 2MB） | ✅ 1880 KB < 2MB（从 2118KB → 1880KB，-238KB） |
| `npm run verify` 步骤 10（品牌资源 RGBA 透明） | ✅ 7 张全部 colorType=6 |
| `npm run verify` 12 步 | ✅ 全部通过 |
| `npm test` 60+ 项 | ⚠️ 仍 1 项失败（`getStats 当前月默认排除未来账单`，属于 Phase 7.2 fake-db aggregate 桩缺失，与本次压缩无关） |

#### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| ECharts 1006KB 仍是主包最大项（54%） | 若后续品牌资源还要加，主包会再次逼近 2MB | 视用户反馈决定是否替换为 wx-charts/uCharts（轻 ~50-150KB） |
| `npm test` getStats 失败 | fake-db.js 缺 `db.collection().aggregate().match().group()` 链式 + 内存聚合 | Phase 7.2 修复 |
| 品牌资源真机视觉未验证 | 仅在 macOS 上视觉抽检 | Phase 10 真机走查时复验 |

#### Phase 7 完成度

- [x] 7.1 主包体积 < 2MB
- [x] 7.2 `getStats 当前月默认排除未来账单` 契约测试通过
- [x] 7.3 复跑 `npm run verify` 12/12 + `npm test` 60+/60+ 全部通过

---

### 2026-08-18 进度记录：Phase 7.2 fake-db aggregate 桩补齐

> 本次更新为本地静态与契约验证（不涉及云端部署/联调/真机），不标记 V1.1 完成。

#### 已完成

**Phase 7.2 fake-db aggregate() / and() 链式桩补齐**

- `scripts/fake-db.js` 新增 `db.collection(name).aggregate().match().group().end()` 链式 + 内存聚合执行器
  - 支持 `$match`（复用现有 `matchValue`）+ `$group`（按 `_id` 表达式分组 + `sum` 累加器）
  - 新增 `db.command.aggregate`：`sum("$field")` / `substr(["$field", start, length])`
  - 新增 `resolveExpr(doc, expr)`：解析 `$field` 字符串 / `__op` 表达式 / 嵌套对象
  - 新增 `runAggregatePipeline(rows, pipeline)`：纯内存执行
- `and()` 链式 bug 修复：`gte().and(lte()).and(lte())` 之前会断（`match.date.and(...)` 失败）
  - 涉及 `listBills` / `getStats` / `getHomeSummary` 3 个云函数共 4 处链式 and
  - 改用递归构造 `makeAndCond(conditions)`，每次 `.and()` 仍可继续链
- `scripts/test-contract.js` 新增 3 个 fake-db 回归测试
  - `fake-db aggregate() 链式：match + group + sum`
  - `fake-db aggregate() 支持 substr 表达式`
  - `fake-db and() 支持链式（gte().and().and()）`

**测试通过情况**

- `getStats 当前月默认排除未来账单`（原 1 项失败）现通过
- 原有 60 项测试全部通过
- 新增 3 项 fake-db 回归测试通过
- 总计：**63/63** 通过

#### 验证结果

| 检查项 | 结果 |
|---|---|
| `npm run verify` 12 步 | ✅ 全部通过 |
| `npm test` 60+ 项 | ✅ **63/63** 通过（原 60 + 新 3） |
| 主包体积 | ✅ 1880 KB < 2MB |
| 品牌资源 RGBA 透明 | ✅ 7 张全部 colorType=6 |

#### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| ECharts 1006KB 仍是主包最大项 | 暂不动；当前 1880KB 余量 168KB，足够支撑 Phase 8-10 | 若加新资源再评估 |
| 品牌资源真机视觉 | 仅 macOS 视觉抽检 | Phase 10 真机走查时复验 |
| 通知/排序 UI/图片凭证等暂缓功能 | 仍是 V1.1+ 计划 | 不在 V1.1 范围 |

#### V1.1 Phase 7-11 进度

- [x] Phase 7 发布前代码清理（verify + test 全绿）
- [x] Phase 8 预检 + 部署文档 + 部署脚本就绪（待用户执行部署动作）
- [ ] Phase 8 实际部署（需用户手动在微信开发者工具执行）
- [ ] Phase 9 真实数据联调
- [ ] Phase 10 真机视觉与图表
- [ ] Phase 11 发版候选

---

### 2026-08-18 进度记录：Phase 8 预检 + Phase 9/10 清单 + 文档收尾

> 本次更新为本地准备（不涉及云端部署/联调/真机），不标记 V1.1 完成。

#### 已完成

**Phase 8 预检与材料准备**
- 删除死代码：`initCategories.js`（被 `ledgerFunctions` 替代）+ 空目录 `billFunctions` + `initFunctions`
- 在 `accountingFunctions/` 执行 `npm install`（xlsx 0.18.5 装好）
- 修复 `npm` 缓存权限问题（用 `--cache /tmp/npm-cache-tmp`）
- 3 个云函数 `node --check` 全部通过
- 确认 `app.js` 环境 ID 与 `envList.js` 一致（`cloud1-d1gq4g6a7c2911b56`）
- 确认 `config.json` 权限配置正确

**新增交付物**
- `PHASE_8_DEPLOY.md`（182 行）—— 11 集合 + 13 索引 + 3 云函数部署操作清单
- `uploadCloudFunction.sh`（重写）—— 用新 CLI 语法，自动部署 3 个云函数
- `PHASE_9_E2E.md`（415 行）—— 18 场景真实数据联调清单
- `PHASE_10_DEVICE.md`（340 行）—— 3 设备 + 8 类真机走查清单
- `scripts/gen-test-xlsx.js` —— 测试 xlsx 生成器（含未来日期 + 重复账单，验证排除和去重逻辑）

**代码审计清理**
- 删除 `miniprogram/pages/stats/index.js` 中 1 个调试 `console.log`
- 确认无 TODO / FIXME / XXX / debug 残留
- 确认所有 brand 图片被引用、无孤儿图片
- 确认所有 pages 目录已注册到 app.json
- 确认所有 .js 页面无 `wx.cloud.database` 直连

**文档收尾**
- `README.md` 更新：新增 V1.1 文档链 + V1.1 Phase 7-11 进度表
- V1.0 MVP 计划文档末尾追加"后续 V1.1 计划"指针

#### 验证结果

| 检查项 | 结果 |
|---|---|
| `npm run verify` 12 步 | ✅ 全部通过 |
| `npm test` 63 项 | ✅ 全部通过 |
| 主包体积 | ✅ 1880 KB < 2MB |
| 3 个云函数语法 | ✅ 全部通过 |
| xlsx 0.18.5 实际可用 | ✅ 写 15KB buffer 成功 |
| 测试 xlsx 端到端生成 | ✅ 62 支出 + 20 收入，含未来日期和重复 |

#### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| Phase 8-10 需用户执行 | 云端部署 / 2 账号联调 / 3 设备真机 — 全部需人工 | 按 3 份清单逐步执行 |
| ECharts 1006KB 仍是主包最大项 | 当前 1880KB 余量 168KB | 若加新资源再评估替换 |
| 排序 UI / 通知 / 图片凭证 / 周期 / 标签 / 收藏置顶 | 全部明确暂缓 | 不在 V1.1 |

#### 交付物清单（V1.1 全部新材料）

| 文件 | 用途 |
|---|---|
| `家庭记账本_V1.1_发布计划与验收目标.md` | V1.1 计划主体（387 行） |
| `PHASE_8_DEPLOY.md` | Phase 8 部署清单（182 行） |
| `PHASE_9_E2E.md` | Phase 9 联调清单（415 行） |
| `PHASE_10_DEVICE.md` | Phase 10 真机清单（340 行） |
| `uploadCloudFunction.sh` | 云函数自动部署脚本 |
| `scripts/gen-test-xlsx.js` | 测试 xlsx 生成器 |
| `scripts/fake-db.js` | 增强 aggregate() / and() 链式桩 |
| `scripts/test-contract.js` | + 3 个 fake-db 回归测试（60→63） |
| `miniprogram/images/brand/*.png` | 7 张压缩（816KB → 579KB） |
| `README.md` | 更新到 V1.1 状态 |
| `家庭记账本_MVP开发计划与高标准验收目标.md` | 末尾追加 V1.1 指针 |
| `PHASE_11_RELEASE.md` | Phase 11 发版收尾清单（复跑 + Release Notes 模板 + 仓库侧动作） |
| `ECHARTS_OPTIMIZATION.md` | V1.2 优化资料（5 方案对比 + 推荐方案 A：uCharts 0.3MB） |
| `V1.2_CANDIDATES.md` | V1.2 候选功能清单（含几千条导入进度条 / 排序 UI / 通知类 / 图片凭证 / 周期记账 等 10 项） |

---

*文档版本：v1.1.0  |  创建日期：2026-08-18  |  最后更新：2026-08-18（Phase 7 全部完成 + Phase 8-10 清单就绪）*
