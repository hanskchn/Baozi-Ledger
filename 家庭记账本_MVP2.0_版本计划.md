# 家庭记账本 MVP 2.0 版本计划

> 文档状态：已确认范围，目标 1 安全加固 + 目标 2 性能还债已闭环部署，进入目标 3（体验补齐）
> 创建日期：2026-09-03
> 定位：基于 2026-09-03 全量代码盘点（前端 19 页 + 云函数 3881 行）的版本评估与规划
> 前置文档：`需求基线.md`（业务规则最高优先级）、`家庭记账本_MVP开发计划与高标准验收目标.md`（V1.0/V1.1）、`V1.2_CANDIDATES.md`
> 版本命名说明：本文所称 MVP 2.0 对应 V1.2_CANDIDATES.md 中的"下一版本"。按基线 §17 约定，暂缓功能需用户重新确认方可实现，见 §5 决策清单。

---

## 一、现状结论（TL;DR）

1. **V1.0/V1.1 功能范围已全部落地**：需求基线 §19 附录中的 16 个章节规则全部"已实现"或按计划暂缓，唯一的 UI 缺口（排序入口）经业务评估明确不做。当前无 P0 级功能缺失。
2. **工程底子扎实，但有 4 个必须优先修复的安全问题**：账单搜索正则注入（ReDoS）、定时器入口可被客户端伪造、云函数错误信息原样透传、偏好写入无校验。均已定位到行级，修复成本低。
3. **最大风险不在功能，在"未验收的存量"**：云端部署版本、双账号联调、真机走查自 V1.1 起持续挂起；测试基线存在 6 个历史失败 + 3 个 verify 告警。MVP 2.0 必须先还债再添新。
4. **建议版本主线**：安全加固 → 性能还债 → 体验补齐 →（用户圈选）功能增强 → 质量回归发版，预计 5 周。**不建议**在本版本直接堆新功能，理由见 §3。

---

## 二、业务结构盘点

### 2.1 业务架构全景

```
┌─ 入口与身份 ──────────────────────────────────────────┐
│ 显式登录（头像/昵称授权）→ 登录态门禁 → initUser 四级优先 │
│ （有效邀请 > 待处理邀请 > 上次账本 > 自动建账本）          │
├─ 账本与协作 ──────────────────────────────────────────┤
│ 多账本切换/创建/改名/解散(30天可还原) │ 邀请码+二维码分享  │
│ 成员管理（50人上限/单管理员/转让/退出/移除）│ 账号注销脱敏  │
├─ 记账核心 ────────────────────────────────────────────┤
│ 记一笔（分类二级/账户/成员/日期时间/金额分校验/版本号并发）│
│ 再记一笔 │ 分类账户记忆（用户+账本维度）│ 左滑删除/编辑    │
├─ 查询与分析 ───────────────────────────────────────────┤
│ 账单列表(分页/筛选/排序) │ 搜索 │ 统计（占比/趋势/成员维度）│
│ 预算（月维度/80%预警/未来月）│ 操作日志（28种action审计）  │
├─ 数据迁移 ────────────────────────────────────────────┤
│ 随手记 xlsx 导入（预览/去重/批次撤销）│ xlsx 导出（临时链接）│
├─ 运营与增长 ───────────────────────────────────────────┤
│ 每日记账提醒（订阅消息3档时间）│ 新功能公告 │ 意见反馈体系  │
│ 隐私弹窗 │ 欢迎引导 │ 邀请裂变                            │
└──────────────────────────────────────────────────────┘
```

### 2.2 功能完成度矩阵（对照需求基线）

| 模块 | 状态 | 说明 |
|---|---|---|
| 登录/初始化/默认账本 | ✅ 已实现 | 含幂等锁、四级优先级 |
| 多账本/成员/邀请/注销 | ✅ 已实现 | 事务化，50 人上限 |
| 记账/编辑/删除 | ✅ 已实现 | 事务 + 版本号并发控制 |
| 分类/账户/预算 | ✅ 已实现 | 软删除/停用/最后保护 |
| 列表/搜索/筛选/统计 | ✅ 已实现 | 服务端聚合；搜索为全量扫描（见 §3.3 P1） |
| 导入/导出/审计 | ✅ 已实现 | 含批次撤销、中途失败回滚 |
| 每日记账提醒 | ✅ 已实现（2026-08-27） | 一次性订阅 + 自动续订 |
| 预算提醒/成员记账通知 | ⏸ 暂缓 | 基线 §14，MVP 2.0 候选 |
| 图片凭证/周期记账/标签/收藏 | ⏸ 暂缓 | 基线 §17，需用户重新确认 |

### 2.3 工程规模与质量门禁现状（2026-09-03 实测）

| 指标 | 数值 | 评价 |
|---|---|---|
| 页面数 | 19 页 + 4 组件，无孤儿页 | 良好 |
| 云函数 | 4 个生产函数（ledger 1677 行 / accounting 1719 行 / feedback 358 行 / resetTestData 127 行，共 3881 行，目标 1/2 后实测） | 中上 |
| 主包体积 | **1164 KB**（上限 2MB，余量 836KB） | 良好（自研 Canvas 图表替代 ECharts 后大幅下降，V1.2 候选 #2 已作废） |
| 自动化测试 | `npm test` 91 项：84 通过 / **7 失败（全部为存量，HEAD 对照零回归）** | 待清偿 |
| 静态验收 | `npm run verify` 13 步：**3 项告警**（feedback 敏感字段/绿色、契约误报等存量，第 13 步开发者白名单一致性已通过） | 待清偿 |
| 云端/真机验收 | 部署记录表为空，双账号 18 场景自 V1.1 挂起至今 | **最大风险** |
| 工作区 | addBill 同名二级分类修复已提交（7c7da8a·2026-09-03）；目标 1 安全加固改动随本计划修订一并提交（见 §6 台账） | ✅ 已闭环 |

---

## 三、五维评估

> 级别定义：P0 = 本版本必须修（安全/数据正确性）；P1 = 本版本应修（影响体验/扩展）；P2 = 可延后。行号为盘点时快照。

### 3.1 安全性 —— 4 个 P0，整体权限体系是好的

**亮点**：写操作全部经云函数 + `cloud.getWXContext()` + `requireMember/requireAdmin` 校验；账单 CRUD 事务化 + 版本号；金额/日期服务端强校验；邀请码不回传 familyId；开发者动作三处白名单生效；前端零直连数据库。

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| S1 | **正则注入/ReDoS**：`listBills` 将用户输入的商家/备注直接作为 `db.RegExp` 的 regexp，未转义 | accountingFunctions/index.js:1287-1288 | P0 |
| S2 | **定时器入口可伪造**：`event.TriggerName` 由客户端可控，任意用户可传 `{"TriggerName":"dailyReminder"}` 触发全量提醒群发（消耗所有用户订阅额度）或触发 `purgeExpiredFamilies`；后者在主 switch 中也对任何用户开放 | ledgerFunctions/index.js:1477-1486, 1464 | P0 |
| S3 | **错误信息泄露**：三个云函数顶层 catch 将 `error.message` 原样返回客户端（可泄露集合名、驱动报错） | ledger:1491、accounting:1571、feedback:343 | P0 |
| S4 | **偏好写入无校验**：`saveBillPreferences` 将 expenseCategory/incomeCategory/account 任意 JSON 直接入库；`avatarUrl` 无长度限制 | accounting:1073-1080、ledger:224 | P0 |
| S5 | 邀请码明文存储 + `Math.random()` 生成（非加密随机）；8 位 32 字符集在线爆破不现实，风险低-中 | ledger:162-168 | P1 |
| S6 | `dissolveFamily` 的 `forceLastFamily` 由客户端自报可绕过"至少保留一个账本"（仅影响自己） | ledger:967-972 | P1 |
| S7 | `verifyInvite`/`confirmJoinFamily` 无速率限制（可被脚本探测有效码） | ledger:794-804 | P1 |
| S8 | `resetTestData` API 设计危险：action 非 `counts` 时默认清空全部 13 个集合（有开发者白名单保护，但建议改为显式 `confirm: true`） | resetTestData:89-97 | P1 |
| S9 | 开发者 openid 硬编码且在 3 处重复，换号需改三处 | feedback:11、resetTestData:7、ledger:1344 | P2 |
| S10 | 部分多步写无事务：`saveBudget` 先查后插可并发重复文档；`renameCategory`/`rollbackImport` 批量更新中断留半更新（有批次兜底） | accounting:520-522、405-413 | P1 |

> **状态（2026-09-03 目标 1）**：S1-S7、S10 已在 ledger/accounting 修复（未部署）；S3 的 feedback 与 S8 的 resetTestData 已修复；S9 通过「verify 13 步」一致性门禁收敛。全部改动经 `npm test` HEAD 对照零回归（存量 7 失败 + 3 告警与 HEAD 完全一致）。


### 3.2 功能性 / 业务结构 —— 无 P0 缺口，两处一致性小洞

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| F1 | `listOperationLogs` 的 actionGroup 分组漏了 budget 组，预算操作无法按组筛选 | accounting:970-976 | P2 |
| F2 | 每日提醒订阅表 `limit(1000)` 无分页，超 1000 用户静默截断 | ledger:1321 | P1（随 S2 一起修） |
| F3 | 排序 UI 缺口维持 V1.1 决策：不做（默认 dateDesc 覆盖 90% 场景） | V1.1 记录 | 关闭 |

### 3.3 性能 —— 单账本家庭场景够用，规模化前有三处硬伤

**亮点**：统计/首页摘要已下沉数据库聚合；分页 + 60s 缓存 + 首页快照乐观增量；自研 Canvas 图表零依赖；账本切换本地化优化完成。

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| P1 | **搜索全量入内存**：`searchBills` 每次把整本账单拉回内存过滤再取 20 条；账单数万条后必超时 | accounting:570-572 | P1 |
| P2 | **批量改名/停用全表扫**：分类/账户 rename/delete 全量拉账单后逐条 `Promise.all` update，应改 `where().update` | accounting:405-413, 449, 476, 500-502 | P1 |
| P3 | **每日提醒串行发送**：单次 limit(1000) 拉订阅、逐用户 4-8 次 DB 往返，数百用户逼近 60s 超时 | ledger:1321, 1326-1337 | P1 |
| P4 | 导入 >500 条逼近超时（V1.2 候选 #1 已有方案 A：客户端分批 200 条/批 + 进度条，~1 人天，低风险） | V1.2_CANDIDATES §1 | P1 |
| P5 | `listFamilies` 等读路径 N+1（每账本一次成员查询） | ledger:530-531 | P2 |
| P6 | bills/index 与 index 页 setData 同时维护两份重复结构（`bills` + `billGroups`），千条账单后是渲染瓶颈 | bills:183-184、index:415-423 | P2 |
| P7 | 主包无分包：19 页全在主包（当前 1164KB 余量充足；testTools/feedback/feedbackAdmin 等低频页可拆分包，为后续功能腾空间） | app.json:2-22 | P2 |
| P8 | 品牌插画 624KB 全部随主包首启加载（可转网络图或分包） | images/brand | P2 |

### 3.4 易用性 —— 核心闭环顺滑，"状态可见性"是最大短板

**亮点**：防重复提交全覆盖（saving/loading/disabled 双绑定）；危险操作确认完整；新手引导接力时序（隐私→欢迎→公告）打磨好；图片降级、空态插画体系完整；搜索 350ms 防抖。

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| U1 | **骨架屏仅 stats 有**：bills/family/account/category/logs 连 loading 提示都没有，account 无任何加载/错误 UI | stats.wxml:32-40 等 | P1 |
| U2 | 下拉刷新不齐：stats 和 family 未开启（stats 只能靠切筛选刷新） | 各 index.json | P1 |
| U3 | bills/logs 触底加载无"加载中/没有更多"底部提示 | bills.wxml | P1 |
| U4 | 搜索无分页（一次拉全量，与 P1 同根）；搜索 loading 态未上屏 | search:17 | P1 |
| U5 | 表单校验为提交时 toast 式，输入过程无即时反馈；金额上限 7 位整数无显式提示 | addBill:484-491 | P2 |
| U6 | 无障碍为零：全项目 0 个 aria 标签，Emoji 图标读屏不友好 | 全局 | P2 |
| U7 | **偏好缓存互踩（真实 bug）**：首页预载会整体覆写 `formOptions:<familyId>` 且置 `preferences: null`，抹掉记账页保存的偏好 → 首页停留 5 分钟后记账，默认分类/账户丢失 | index:328-335 vs addBill:177-196 | **P1（当作 bug 修）** |

### 3.5 工程质量 / 可维护性 —— 测试文化好，重复代码是主要债务

**亮点**：83 项单元+契约测试、12 步静态验收门禁、action↔handler 契约自动比对、操作日志 28 种 action 全覆盖、无 TODO/console.log 残留。

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| Q1 | 存量 6 个测试失败 + 3 个 verify 告警（getStats 聚合 3 例、leaveFamily/dissolveFamily 4 例、feedback 绿色 + 契约误报），已确认与新增代码无关，但长期挂着会淹没真回归 | scripts/ | P1 |
| Q2 | 无统一请求层：`wx.cloud.callFunction` 包装在 19 个文件各写一遍；统一层可集中做超时、错误码映射、日志上报 | bills:528 等 19 处 | P1 |
| Q3 | 上海时区日期工具在 9 个文件重复 19 处；左滑手势 75 行×2 逐行重复；时间选择弹层在 stats/bills/filter/export 四处重复 | index:466 等 | P2 |
| Q4 | addBill 重复定义同名方法（`onDateSheetChange` 等被静默覆盖） | addBill:295-308 | P1 |
| Q5 | billDetail 死代码：`editing/saveEdit/cancelEdit` WXML 从未引用 | billDetail:26-38 | P2 |
| Q6 | envList.js 死文件；`billFunctions/initFunctions` 空目录残留 | miniprogram/、cloudfunctions/ | P2 |
| Q7 | feedbackFunctions/resetTestData 无 package-lock.json；resetTestData 的 wx-server-sdk ~2.6.3 与其他 ~2.4.0 不一致，部署不可复现 | 各 package.json | P2 |
| Q8 | 契约校验只覆盖 ledger/accounting，feedback/resetTestData 的 action 不在门禁内（verify 第 8 步的 9 个"误报"实为盲区） | verify.js | P2 |

---

## 四、MVP 2.0 版本计划

### 4.1 版本定位

**MVP 2.0 = "加固版"**：不追求功能面扩张，把 V1.x 欠下的安全、性能、体验、质量债在一个版本内清偿，并完成自 V1.1 挂起至今的云端/真机验收闭环，为后续"功能版"（2.1+）打地基。

**版本目标（可衡量）**：
1. 安全项 S1-S10 全部修复并新增回归测试用例（已新增 8 项，进入常驻基线）。
2. `npm test` 从 83 项/6 失败 → ≥90 项/0 失败；`npm run verify` 13 步 0 告警。
3. 双账号 18 场景云端联调 + 真机走查**闭环并归档**（V1.1 遗留清零）。
4. 搜索、改名批量操作、每日提醒在 1 万条账单 / 500 订阅用户量级下实测不超时。
5. 用户圈选的 1-2 个功能增强项上线（见 §5）。

### 4.2 范围与排期（1 人 + AI 协作，预计 5 周）

| Phase | 内容 | 对应项 | 工作量 | 里程碑物 |
|---|---|---|---|---|
| **A. 安全加固周**（第 1 周） | 正则转义；TriggerName 与主 switch 鉴权（定时器判断加 secrets 校验或独立触发器函数）；错误码收敛为稳定业务码不透 message；saveBillPreferences/avatarUrl 服务端校验；resetTestData 改显式确认 + 开发者白名单一致性门禁；邀请码改 `crypto.randomInt`；saveBudget 确定性文档 ID 消竞态；新增 8 项安全契约测试 | S1-S10 | 4-5 天（✅ 代码已完成，91 项测试 84 通过、存量 7 失败零回归；⏳ 部署待用户打开微信开发者工具） | 云函数重部署 + 安全测试全绿 |
| **B. 性能还债周**（第 2 周） | searchBills 改数据库聚合/分页；分类账户批量操作改 `where().update`；dailyReminder 分页 + 小并发（并加 1000 截断保护）；导入分批 200 条/批 + 进度条（V1.2 候选 #1 方案 A） | P1-P4, F2 | 5 天 | 大数据量实测记录归档 |
| **C. 体验补齐周**（第 3 周） | 修复偏好缓存互踩（U7，最高优先）；全页面 loading/骨架屏补齐；下拉刷新补齐 stats/family；分页 footer；搜索 loading 上屏；addBill 重复方法清理；billDetail 死代码清理；建立统一 callFunction 请求层（先覆盖新增代码，存量渐进迁移） | U1-U5, U7, Q2, Q4, Q5 | 5 天 | 易用性走查清单 |
| **D. 功能增强周**（第 4 周，可选） | 从 §5 决策清单中用户圈选 1-2 项实施（推荐：分类预算 + 预算提醒，成本低且与现有预算模块强关联） | 用户圈选 | 3-5 天 | 功能验收 |
| **E. 质量回归与发版周**（第 5 周） | 清偿存量 7 失败 + 3 告警（Q1）；契约门禁扩展到 feedback/resetTestData（Q8）；lock 文件与 sdk 版本统一（Q7）；删除死文件/空目录（Q6）；双账号 18 场景联调 + 真机走查（挂起项闭环）；主包体积复测（addBill 修复已提前提交：7c7da8a） | Q1, Q6-Q8 + 外部验收 | 5 天 | 发版候选 + 部署记录表归档 |

**明确不做（本版本）**：排序 UI（维持 V1.1 决策）、分包/图片网络化（P7/P8，主包余量充足，留给 2.1）、无障碍改造（U6）、多账本统计混合等基线未确认需求。

### 4.3 验收标准（沿用项目门禁）

- 每次云函数改动：`npm run check`（verify 13 步 + test）无新增失败/告警（存量 7 失败 + 3 告警记录在案）后才部署，部署记录写入 MVP 计划 §7 表格。
- Phase A 新增安全测试用例进入 `npm test` 常驻基线。
- 发版门槛：0 安全项遗留、0 测试失败、0 verify 告警、双账号+真机验收归档、主包 < 2MB。
- 视觉/交互验收沿用 `家庭记账本_MVP开发计划与高标准验收目标.md` §4.3-4.5 标准。

### 4.4 风险与依赖

| 风险 | 应对 |
|---|---|
| 云函数改安全逻辑可能影响线上用户（每日提醒已上线） | Phase A/B 分函数灰度部署；部署前快照 reminder_subscriptions 数据；43101 恢复逻辑回归 |
| `where().update` 批量更新与事务语义在云开发 SDK 的支持性需实测 | 先在测试环境验证，不支持则退回逐条更新（保留现状，仅做预取优化） |
| 双账号/真机验收长期挂起，可能阻塞发版里程碑 | 提前到第 1 周就绪测试账号与环境，Phase E 只做回归而非首次联调 |
| 用户未圈选功能增强项 | Phase D 设计为可裁剪，默认跳过并入 2.1 |
| 存量 7 失败修复可能牵出老逻辑问题 | 单独分支处理，逐一确认是"测试过时"还是"实现缺陷"，不确定时回到需求基线裁决 |

---

## 五、决策清单（启动时已确认 + 遗留决策点）

按 `需求基线.md` §17 与 V1.2_CANDIDATES.md 约定，以下事项需用户明确确认后才能进入开发：

1. **功能增强圈选**（Phase D 做不做、做哪些）：
   - ☑ **本期不做功能增强，全力还债**（2026-09-03 已确认）
   - ☐ 分类预算（~1 人天）｜☐ 预算提醒+成员记账通知（~2 人天，基线 §14）｜☐ 账单图片凭证（~3-5 人天，基线 §17）｜☐ 周期记账（~2 人天，基线 §17）｜☐ 年度总结/同比环比（~2-3 人天）
2. **MVP 2.0 的验收环境**：✅ 已确认——双测试账号与真机（iPhone 刘海屏 + Android 窄屏）在 Phase E（目标 5）前准备完毕，不阻塞当前开发。
3. **每日提醒线上数据**：⏳ 待目标 2（性能还债）启动时确认，决定 Phase B 中 P3 的优先级权重。

---

## 六、执行台账（6 目标 × 测试门禁）

| 目标 | 名称 | 状态 | 门禁结果 |
|---|---|---|---|
| 0 | 提交现有修复（addBill 同名二级分类） | ✅ 已提交 `7c7da8a` | 无回归 |
| 1 | 云函数安全加固 S1-S10 | ✅ 代码完成、门禁通过；⏳ 部署统一到目标 5 | `npm test` 91 项 84 过 / 存量 7；`verify` 13 项 3 存量 |
| 2 | 性能还债：searchBills 聚合分页、分类账户批量 `where().update`、dailyReminder 分页+小并发、导入分批 200 条 | ✅ 代码完成、门禁通过；⏳ 部署中 | `npm test` 108 项 101 过 / 存量 7；`verify` 13 项 3 存量告警 |
| 3 | 体验补齐：U7 偏好缓存互踩（最高优先）、loading/下拉刷新/分页 footer 等 | ⏳ 未启动 | — |
| 4 | 质量回归：清偿存量 7 失败 + 3 告警、契约门禁扩展 feedback/resetTestData、Q6/Q7 清理 | ⏳ 未启动 | — |
| 5 | 部署 + 双账号 18 场景 + 真机走查 + 发版 | ⏳ 待用户打开微信开发者工具并登录、Phase E 验收环境就绪 | 部署记录见 §7 |

> 约定：每个目标完成后跑一次门禁（无新增失败/告警）并提交一次（不主动推送）；云函数部署与最终验收统一放目标 5。

## 七、部署记录表

| 日期 | 云函数 | 部署原因 | 部署前门禁 | 部署结果 | 备注 |
|---|---|---|---|---|---|
| 2026-09-03 | ledgerFunctions（38.0 KB） | 目标 1 安全加固 | `npm test` 91 项（存量 7 零回归）+ `npm run verify` 13 步（存量 3 项记录在案） | ✅ 部署成功（IDE CLI） | 定时器伪造/邀请限流/注销意图等；待回归 9/13/21 定时提醒 |
| 2026-09-03 | accountingFunctions（38.3 KB） | 目标 1 安全加固 | 同上 | ✅ 部署成功（IDE CLI） | 正则转义/错误收敛/偏好校验/预算竞态；测试见 §验证清单 |
| 2026-09-03 | feedbackFunctions（4.5 KB） | 目标 1 安全加固 | 同上 | ✅ 部署成功（IDE CLI） | 错误信息收敛 |
| 2026-09-03 | resetTestData（2.3 KB） | 目标 1 安全加固 | 同上 | ✅ 部署成功（IDE CLI） | 清除需显式 `confirm:true`；测试工具页验证 |
| 2026-09-03（补丁） | ledgerFunctions（38.4 KB） | 邀请限流升级为数据库全局计数，修复多实例绕过 | `npm test` 91 项零回归 + `verify` 13 步存量 3 告警 | ✅ 部署成功（IDE CLI） | `rate_limits` 集合新引入；13 次连续调用第 13 次应拒绝 |
| 2026-09-03（补丁） | resetTestData（2.3 KB） | `ALL_COLLECTIONS` 纳入 `rate_limits` | 同上 | ✅ 部署成功（IDE CLI） | 测试环境清除数据时联动清理限流计数 |
| 2026-09-03（补丁） | accountingFunctions（38.4 KB） | 偏好校验改为对象形状白名单（原只允许字符串导致前端偏好被拒） | `npm test` 92 项零回归 + `verify` 13 步存量 3 告警 | ✅ 部署成功（IDE CLILI） | 复测：记类别保存→重启→最近使用恢复 |
| 2026-09-03（补丁2） | accountingFunctions（38.7 KB） | 写读偏好收敛到单一确定性主档，修复历史随机 id 文档并存导致读回旧值 | `npm test` 94 项零回归 + `verify` 13 步存量 3 告警 | ✅ 部署成功（IDE CLI） | 复测：收入态保存后立即读回应为新值 |

---

*MVP 2.0 · 2026-09-03 · 基于 19 页面前端 + 3881 行云函数全量盘点（目标 1/2 加固后更新）*
