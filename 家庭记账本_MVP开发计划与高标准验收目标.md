# 家庭记账本 MVP 开发计划与高标准验收目标

> 文档状态：执行中
> 初始版本：2026-08-11
> 维护规则：每完成一个阶段，更新“进度记录、风险与验收结果”；任何未达标项不得标记为完成。

## 1. 项目目标

在微信开发者工具和云开发测试环境中交付可真实使用的家庭共享记账 MVP。产品不仅要“功能可用”，还必须达到可发布质量：视觉统一、信息层级清晰、交互稳定流畅、真机适配完整、账务数据准确、权限边界可靠、失败可恢复。

已确认范围：先完成 MVP；全产品统一奶黄暖橙视觉；品牌插画采用精简 ImageGen 资源包；通知功能延后；首页 FAB 直接进入默认支出记账；分类图标继续使用 Emoji；图片凭证、周期记账、标签、收藏置顶等继续暂缓。

## 2. 开发阶段

### Phase 0：基础收敛与数据安全
- 统一云函数 action 分发并兼容旧 type，所有前端调用使用 action。
- 收敛账务云函数入口，统一错误格式、成员/管理员校验和跨账本隔离。
- 补齐账单版本号、软删除、创建者、成员快照、导入批次/指纹与不可变操作日志。
- 统一金额分存储、双端金额校验、Asia/Shanghai 日期工具、未来账单统计口径。
- 完成集合创建、权限最小化和查询索引配置。

### Phase 1：统一视觉系统与品牌资源
- 在 app.wxss 建立颜色、字号、间距、圆角、阴影和安全区设计令牌。
- 统一页面壳、加载态、错误态、空态、底部弹层、二次确认、账单行和金额显示。
- 使用 ImageGen 生成并审核 Logo、首页空状态、欢迎引导、搜索无结果、导入状态、通用网络异常等 5-7 个资源。
- 资源统一保存到 miniprogram/images/brand/；透明图先用纯色背景生成、抠图、Alpha 校验。
- 若现有图片在目标组件中出现裁切、模糊、留白失衡、比例不符、主体不清或风格不统一，必须重新通过 ImageGen 生成适配版本，禁止用拉伸变形勉强上线。

### Phase 2：首页与核心记账闭环
- 实现自定义品牌导航、账本切换底部选择器、2x2 概览卡、日期分组最近 10 条账单、下拉刷新、空态/错误态。
- FAB 直接跳转默认“支出”表单，表单内可切换收入。
- 完善新增、连续记账、分类/账户记忆、金额/日期/字段校验。
- 新增账单详情/编辑页，删除二次确认，版本冲突拒绝覆盖。

### Phase 3：家庭协作与账本设置
- 完成多账本创建、切换、命名、成员管理、退出、移除、管理员转让。
- 完成邀请验证、确认加入、待处理邀请、幂等加入、人数上限和旧邀请码失效。
- 完成用户资料、分类、账户和预算管理及管理员权限限制。

### Phase 4：账单、搜索与统计
- 完成账单分页、刷新、滚动加载、筛选、排序和搜索。
- 完成月/年/自定义范围统计、分类排行/占比、趋势、成员维度筛选。
- 接入 ECharts，图表颜色和空态统一视觉系统。

### Phase 5：导入、导出与审计
- 完成随手记 xlsx 上传、预览、全量校验、成员映射、重复检测、批次写入和撤销。
- 完成筛选导出 xlsx、短时下载链接和操作日志。

### Phase 6：质量验证与发布候选
- 云函数单元/契约测试、双成员云端联调、真机检查、性能检查和回归测试。
- 每次云函数变更记录部署函数、版本和验证结果。

## 3. ImageGen 资源策略

### 必须生成
- 品牌包子 Logo：导航栏使用，需提供适合 36-48px 小尺寸的清晰版本。
- 首页无账单插画：适合 120-180px 容器，主体居中，透明背景。
- 欢迎引导插画：适合 240-320px 宽卡片，移动端裁切安全。
- 搜索无结果插画：适合窄屏居中，不能依赖细小细节。
- 导入空态与完成态插画：分别服务选择文件和成功结果。
- 通用网络异常插画：适合错误页，不能遮挡重试按钮。

### 不使用 ImageGen 的内容
- 分类 Emoji、TabBar 线性图标、箭头、关闭、编辑、删除等简单 UI 图标优先使用现有本地资源或代码/矢量实现。
- 不生成带精确中文文案的图片，文案由 WXML 渲染，保证清晰度和可翻译性。

### 资源验收
- 每项资源至少验证透明边缘、实际尺寸、DPR 清晰度、压缩后体积、浅色/深色背景对比和真机裁切。
- 组件比例不合适时重新生成目标尺寸/构图明确的变体；不允许 CSS 强行拉伸导致主体变形。
- 项目引用的最终资源必须落在仓库内，不能只留在 CODEX_HOME/generated_images。

## 4. 发布级高标准验收

### 4.1 功能完整性
- 核心流程无阻断：登录初始化、创建/加入账本、切换账本、记支出/收入、编辑、删除、搜索、筛选、统计、预算、导入、导出均可完成。
- 新增账单保存成功后首页、账单页、统计页和预算页数据一致刷新；失败时输入内容不丢失且可重试。
- 删除必须二次确认；删除后默认列表、统计、预算立即排除，历史审计信息保留。
- 普通成员只能编辑/删除自己创建的账单；管理员可操作全部账单；服务端校验不可被前端绕过。

### 4.2 数据正确性与安全
- 金额只能为正数、最多两位小数、单笔不超过 9,999,999.99 元，前后端结果一致，分存储无浮点误差。
- 所有统计按 Asia/Shanghai 自然日/月计算；未来账单只在选定范围包含未来日期时参与。
- 任一业务查询都必须绑定当前 familyId 和成员权限，不得串账本、读到失效成员数据或泄露 openid/邀请码明文。
- 并发编辑/删除版本冲突必须拒绝覆盖并提示刷新；初始化、邀请、默认数据和导入必须幂等。
- 云函数异常、网络中断、权限不足、空数据、无更多数据均有明确可恢复状态。

### 4.3 视觉与交互品质
- 首页及所有核心页面遵循同一设计令牌，不出现旧绿色/纯黑/冷灰与暖橙体系冲突的残留样式。
- 视觉层级清楚：标题、金额、辅助信息、操作入口在 1 秒内可辨识；长账本名、长备注、极大金额不得溢出、遮挡或撑坏布局。
- 所有可点击区域有明确反馈；按钮防重复提交；底部弹层、选择器和二次确认可关闭且不残留遮罩。
- 图表、空态、错误态和加载态不是装饰占位，而是信息完整、比例稳定、可读可操作。
- 不使用模糊、拉伸、裁切失衡或风格不一致的图片；不适配的图片必须重新 ImageGen 或重新导出。

### 4.4 真机适配
- 至少验证 iPhone 刘海屏、小屏和大屏，以及 Android 常见窄屏；检查状态栏、微信胶囊、自定义导航、原生 TabBar、底部安全区。
- 旋转/不同系统字体缩放不会造成关键文字重叠；滚动区域、键盘顶起、底部 FAB 与 TabBar 不互相遮挡。
- 图片在 1x/2x/3x 密度下清晰，所有本地路径有效，首屏不出现空白破图。

### 4.5 性能与流畅度
- 首屏加载、账本切换、账单刷新、打开表单、弹层开合无明显卡顿；连续快速点击不会重复创建账单。
- 账单页服务端分页，禁止一次读取全部账单；滚动加载有节流且不会重复/跳项。
- 图表仅在需要时初始化，页面离开后不持续占用资源；图片压缩合理，首屏资源不过度膨胀。
- 网络慢、失败和重试路径可感知，避免无反馈等待和重复请求。

### 4.6 代码与部署质量
- JS/WXML/WXSS 符合项目双引号、2 空格、async/await 和 wx:key 约定；不遗留核心链路 TODO。
- 云函数入口动作、错误码、权限校验和返回结构有稳定契约；修改后可明确上传部署。
- 关键业务至少有自动化或可重复手工测试记录；发布前 git diff 无意外资源和调试文件。

## 5. 验收门槛

以下任一项不满足，不得标记 MVP 完成：
- 存在 P0/P1 功能、数据安全、权限或账本隔离缺陷。
- 存在核心页面文字重叠、按钮不可点、图片破损、关键流程卡死或明显误导。
- 真机安全区、键盘、TabBar、弹层或导航适配失败。
- 统计金额与账单明细不一致，或存在时区/未来账单口径错误。
- 任一图片资源因比例不适配而依赖拉伸、遮挡或明显模糊。
- 云端测试环境未完成管理员与普通成员双账号验收。

## 6. 进度记录

| 日期 | 阶段 | 状态 | 验收结果 | 风险/后续 |
|---|---|---|---|---|
| 2026-08-13 | Phase 5 导入契约测试（preview/confirm/rollback） | 进行中 | 新增 `scripts/test-import.js`（6 项，xlsx 桩 + 内存假库）：预览成功并给出成员映射、拒绝无效日期/金额、非管理员确认导入被拒、确认导入自动创建缺失分类/账户、重复导入按指纹去重、撤销仅删除该批次并清理自动创建的分类/账户，全部通过；统一入口 `npm test` 现共 46 项通过 | 真实随手记 xlsx 文件往返仍需在云端实测（本测试为 xlsx 桩）；ImageGen 资源、ECharts 真机、真机布局、云端双账号仍待外部环境 |
| 2026-08-13 | Phase 6 WXML 规范审计 + 一键验证入口 | 进行中 | 审计全部 WXML：`wx:for` 均带 `wx:key`，所有 `bind/catch` 事件处理器均在页面 JS 中定义，零违规；`scripts/verify.js` 新增第 5 步「WXML 规范」门禁（现共 9 步）；新增根目录 `package.json` 提供 `npm test`（40 项单元+契约测试）与 `npm run verify`（9 步静态验收）一键入口；全部通过 | 云端双账号实测、ImageGen 品牌资源、真实 xlsx 往返、ECharts 真机、真机布局走查仍待外部环境 |
| 2026-08-13 | Phase 1 视觉令牌一致性回归 | 进行中 | 审计全部页面 WXSS 色彩：确认整体符合暖橙/奶黄体系，绿色仅用于收入语义（#4CAF50/#16885f/#E8F5E9），红色仅用于删除/危险操作，灰色均为中性文字层级；`scripts/verify.js` 新增第 5 步「视觉系统一致性」门禁（按 HSL 检测非收入语义的绿色强调色），当前零命中；8 步静态验收与 40 项测试全部通过 | 最终视觉仍以微信开发者工具/真机截图巡检为准；ImageGen 品牌资源、云端双账号、真实 xlsx、ECharts 真机仍待外部环境 |
| 2026-08-13 | Phase 6 initUser 初始化幂等契约测试 | 进行中 | 新增 `scripts/test-init.js`（3 项）：首次登录仅创建一个默认账本/一条有效成员/邀请码并初始化默认分类与账户；重复调用幂等返回同一账本不重复创建、用户记录唯一；两个独立用户各自拥有独立默认账本。统一入口 `node scripts/test.js` 现共 40 项全部通过 | 假数据库为内存非隔离实现，验证逻辑分支；云端真实并发/事务/权限仍需双账号实测；ImageGen 资源、真实 xlsx 往返、ECharts 真机、真机布局仍待外部环境 |
| 2026-08-13 | Phase 6 ledgerFunctions 契约测试（邀请/成员/管理员） | 进行中 | 新增 `scripts/test-ledger-contract.js`：对 ledgerFunctions 做 9 项契约测试——管理员不能转让给自己、转让成功且唯一管理员并更新 adminOpenid、非管理员转让被拒、已是成员重复加入幂等不新增、新成员加入成功为普通成员、第 51 位成员被拒、过期邀请被拒、创建邀请仅管理员、管理员不能退出须先转让，全部通过；统一入口 `node scripts/test.js` 现共 37 项通过 | 假数据库为内存非隔离实现，验证逻辑分支；云端真实并发/事务/权限仍需双账号实测；ImageGen 资源、真实 xlsx 往返、ECharts 真机、真机布局仍待外部环境 |
| 2026-08-13 | Phase 6 主包瘦身与体积门禁 | 进行中 | 删除 23 个无引用的快速开始模板图片（create_cbr/ai_example/env-select/database/cloud_dev 等根目录图及 close/copy/examples/setting/question/share/customer-service 等死图标），保留被引用的 tabBar 图标与 avatar；主包目录由约 2.5MB 降至 **1,205,978 字节（约 1.15MB）**，满足发布级「主包 < 2MB」门槛且无悬空引用；`scripts/verify.js` 新增第 6 步「主包体积 < 2MB」门禁（当前 1178 KB） | 品牌资源落地后需复测主包仍 < 2MB（当前余量约 0.85MB）；云端/真机/ImageGen/真实 xlsx 仍待外部环境验证 |
| 2026-08-13 | Phase 6 权限/并发/隔离契约测试 | 进行中 | 新增内存假云数据库 `scripts/fake-db.js`（支持 where/limit/skip/orderBy/get/add/doc.set/update/remove/runTransaction/command 与 where().update）与 `scripts/test-contract.js`：对 accountingFunctions 安全关键处理器做 10 项契约测试——普通成员只能为自己记账、无权编辑/删除他人账单、版本冲突拒绝覆盖、管理员编辑自增版本、跨账本隔离（伪造 familyId 拒绝）、非成员拒绝写入、分类仅管理员、已解散账本成员拒绝写入，全部通过；统一入口 `node scripts/test.js` 现共 28 项通过 | 假数据库为内存非隔离实现，仅验证逻辑分支；云端真实并发/事务语义仍需双账号实测；ImageGen 资源、真实 xlsx 往返、ECharts 真机、主包体积复测仍需外部环境 |
| 2026-08-13 | Phase 6 契约一致性与我的页审计 | 进行中 | 新增 `scripts/verify.js` 第 5 步「前端 action ↔ 云函数 handler 契约一致性」：自动比对全部 46 个前端调用动作与两个云函数 handler（含条件式传参 deleteCategory/setCategoryEnabled、deleteAccount/setAccountEnabled），保证改动不会静默破坏前后端契约；审计 profile（我的）页与 `updateUserProfile`/`getAccountCancellationStatus`/`dissolveFamily`/`cancelAccount` 云端契约，注销/转让/解散/幂等均符合基线；全量语法、18 项单元测试、6 步静态验收全部通过 | 云端契约/并发/权限双账号实测、真实 xlsx 往返、ECharts 真机、ImageGen 资源落地、主包体积复测仍需外部环境验证 |
| 2026-08-13 | Phase 6 单元测试扩展（ledgerFunctions） | 进行中 | 新增 `scripts/test-ledger-pure.js`（6 项）+ 统一入口 `scripts/test.js`（现共 18 项全通过）；`ledgerFunctions` 抽取纯函数 `computeDefaultFamilyName`（默认账本命名递增规则）与 `generateInviteCode`（8 位邀请码、排除 0/1/I/O 易混淆字符）并接入生产路径，新增仅测试用 `module.exports.testUtils`（不影响云端 main）；测试覆盖稳定用户/成员 ID、名称规范化、默认账本命名、邀请码格式、成员规范排序 | 云端契约/并发/权限与真机、ImageGen 资源仍需外部环境实测；单元测试仅覆盖纯函数，不含 DB 集成 |
| 2026-08-13 | Phase 6 单元测试与可重复回归 | 进行中 | 新增 `scripts/test-pure.js`（Node 内置测试器，桩掉 wx-server-sdk/xlsx）对账务核心纯逻辑做 12 项单元测试：金额解析/边界、日期/时间校验、月份边界含跨年、上海时区跨日、文本规范化、稳定文档 ID、成员规范排序、导入成员映射（唯一/重复/未匹配）全部通过；`accountingFunctions` 增加仅测试用的 `module.exports.testUtils`（不影响云端 main）；新增 `scripts/verify.js` 一键静态验收（语法、前端不直连库、无 openid 残留、WXML 配平、git 空白）全部通过 | 云端契约/并发/权限与真机、ImageGen 资源仍需外部环境实测；当前单元测试仅覆盖纯函数，不含 DB 集成 |
| 2026-08-13 | Phase 3 邀请链路 + 页面审计 | 进行中 | 修复 App 已在后台运行时通过分享链接重新进入不处理 `inviteCode` 的问题：`app.js` 新增 `onShow` 读取 query 并入队邀请码、复位初始化；审计 family/分类/账户/预算/账单详情页，管理员权限均由云端 `requireAdmin` 强制，预算写入格式/上限/幂等均服务端校验，未发现新的静态缺陷 | 待云部署后实测“后台运行收到分享邀请”场景与双账号权限；ImageGen 资源、ECharts 真机、真实 xlsx 往返仍待外部环境验证 |
| 2026-08-13 | Phase 6 静态审计 + 分页可恢复性 | 进行中 | 审计 `ledgerFunctions`/`accountingFunctions` 成员校验、账本隔离、账单事务与版本冲突、统计时区/未来账单口径、导入撤销清理，均符合基线；修复账单页与日志页“加载更多失败后 loadingMore 卡死且偏移已推进、重试跳页”的问题，改为失败回滚偏移并复位加载态，满足失败可恢复验收项 | 仍待云部署后的双账号并发/权限实测、真实 xlsx 往返、ECharts 真机与 ImageGen 资源落地 |
| 2026-08-13 | Phase 1 品牌资源接入 + 静态回归 | 进行中 | 已建立 `utils/brand.js` 品牌资源清单（available 开关 + 各插画路径），首页 Logo/空账单插画改为由该清单驱动；新增首次创建账本的欢迎引导（可跳过）；搜索无结果、导入空态/完成态、通用网络异常均已接插图并保留文字降级；全量 `node --check`、无前端直连数据库、无 openid 残留、`git diff --check` 全部通过 | 资源 PNG 尚未生成（本会话无内置 image_gen，Hermes 后端未运行），`available` 保持 false；待资源落地后置 true 并做 Alpha/尺寸/DPR/真机验收 |
| 2026-08-11 | 计划建立 | 进行中 | 已确认 MVP、视觉范围、ImageGen 策略与通知延期 | 开始 Phase 0 |
| 2026-08-11 | Phase 0/2 首轮 | 进行中 | 已统一基础暖橙主题、原生 TabBar 颜色、首页自定义导航、FAB 直达支出表单、下拉刷新配置；已修复首页家庭详情调用使用 action | 继续补齐账本切换面板、首页分组交互、账单详情编辑与云端数据契约 |
| 2026-08-11 | Phase 2/3/4 第二轮 | 进行中 | 已接入首页账本切换底部面板、日期分组账单、账单详情读取/编辑/删除及版本校验、账单分页、搜索、分类/账户/预算管理员接口与页面骨架；首页统计改用服务端聚合避免分页少算 | 仍需补齐导入导出、成员确认邀请、分类编辑、统计图表、操作日志管理页、ImageGen 最终资源与云端双账号/真机验收 |
| 2026-08-11 | Phase 3/4 第三轮 | 进行中 | 已补账单筛选实际选择器、分类/账户重命名服务端校验与页面入口、普通成员退出账本；所有新增写操作记录基础审计日志 | 仍缺邀请确认/待处理流程、导入导出、图表、完整分类二级编辑、日志查询页与真机云端验收 |
| 2026-08-11 | Phase 3 邀请链路 | 进行中 | 邀请已改为“服务端验证 -> 用户确认 -> 服务端加入”；重新生成邀请码会作废旧码，加入时检查 50 人上限，重复加入保持幂等 | 待处理邀请、分享链接解析与管理员昵称展示仍需后续接入 |
| 2026-08-11 | Phase 5 导入首轮 | 进行中 | 已接入 xlsx 云函数依赖、微信文件选择/云存储上传、服务端工作表校验和预览、确认导入、重复指纹跳过、导入批次标识；不存在的分类、二级分类和账户可自动创建 | 仍需补成员映射确认、整批事务语义、批次撤销、导出 xlsx 与云端实测 |
| 2026-08-11 | Phase 5 导入导出第二轮 | 进行中 | 已增加导入批次撤销、管理员 xlsx 导出、云存储临时下载链接与端侧文档打开；导入/导出均写入操作日志 | 仍需做成员映射确认、撤销后自动创建分类/账户清理、整批事务与云端实测 |
| 2026-08-11 | Phase 4 统计与审计首轮 | 进行中 | 统计页已由占位图升级为真实分类占比排行、收入/支出切换和每日收支趋势；新增管理员操作日志查询页，支持分页和刷新 | 当前无 ec-canvas 运行库，需后续引入 ECharts 并在真机验证后替换为 Canvas 图表；日志筛选和操作人展示待补 |
| 2026-08-11 | Phase 0 数据一致性加固 | 进行中 | 统计聚合改为分页读取全部匹配账单，避免超过单页上限少算；账单编辑复用分类、账户、日期和类型的服务端校验 | 仍需云端大数据量压测与必要索引验证 |
| 2026-08-11 | Phase 1 资源接入准备 | 进行中 | 已建立品牌资源目录与首页 Logo/空账单插画引用和失效降级；可直接替换为 ImageGen 审核后的 PNG | 当前会话没有可调用 ImageGen 工具，需生成工具可用后补入资源并完成 Alpha/尺寸/真机验证 |
| 2026-08-11 | Phase 3/5 权限与审计完善 | 进行中 | “我的”页已按角色隐藏预算、分类、账户、导入导出和日志入口；管理员可改账本名；日志页支持基础类型筛选 | 还需操作人和日期范围筛选、账本改名操作日志、云端双账号权限实测 |
| 2026-08-11 | Phase 3 用户资料与命名规则 | 进行中 | 用户可修改昵称并同步当前有效成员关系；创建/改名账本增加用户维度的重名校验；资料和改名均写入操作日志 | 头像修改、注销流程、云端跨成员显示同步仍需验收 |
| 2026-08-11 | Phase 3 分类与账户一致性 | 进行中 | 分类页已补齐一级/二级分类编辑、停用和恢复入口；服务端停用一级分类会同步二级分类，分类和账户改名会同步未删除历史账单的展示字段 | 仍需补物理删除规则、名称规范化字段迁移与云端大账本批量更新实测 |
| 2026-08-11 | Phase 0/4 时区与统计口径 | 进行中 | 首页、预算和统计页默认月份改按 Asia/Shanghai 计算；服务端月统计使用精确下月起始边界、整数分聚合，并默认排除未来账单 | 自定义日期范围、按年统计和显式“包含未来”筛选仍待完成 |
| 2026-08-11 | Phase 2 账单编辑闭环 | 进行中 | 账单详情的编辑入口已改为复用记账表单；编辑可修改收支类型、金额、分类、账户、成员、日期、商家和备注，并继续以账单版本号做服务端并发校验 | 连续记账、编辑后的跨页面刷新及双成员权限实测待补 |
| 2026-08-11 | Phase 3 配置恢复与未来统计 | 进行中 | 账户管理可展示、停用和恢复全部账户；新增二级分类会拒绝停用的一级分类；服务端统计对未来整月自动包含未来账单、当前月仍只统计已发生数据 | 物理删除/自动清理规则、范围统计和真机云端实测待完成 |
| 2026-08-11 | Phase 0/3 家庭入口审计 | 进行中 | 已移除会无保护删除分类/账户的“重置数据”入口及云端动作；手动创建账本自动生成有效邀请码，家庭创建、邀请、加入、移除、退出和管理员转让均写入审计日志 | 待处理邀请、分享链接解析、邀请码撤销入口和管理员昵称展示仍待完成 |
| 2026-08-11 | Phase 1 视觉令牌收敛 | 进行中 | 记账、账单、预算、搜索、家庭、导入、账户和分类核心页面已将旧绿色主操作与冷灰残留替换为暖橙/奶黄/暖棕体系；绿色仅保留收入语义 | 仍需微信开发者工具截图巡检、TabBar 本地图标替换和 ImageGen 品牌资源落地 |
| 2026-08-11 | Phase 0 多账本隔离加固 | 进行中 | 修复账本切换后全局账本详情未同步的问题；首页和家庭管理切换时先验证成员权限并更新全局当前账本，记账表单始终读取当前账本 ID | 仍需云端双账号在连续切换、后台恢复与编辑账单场景下实测隔离性 |
| 2026-08-11 | Phase 3 删除规则 | 进行中 | 分类/账户管理改为服务端判定：未被账单使用时物理删除，已使用时自动停用；保留最后一个可用账户和一级分类最后一个可用二级分类的保护 | 尚需云端验证历史账单引用与大量账单下的性能表现 |
| 2026-08-11 | Phase 4 账单筛选 | 进行中 | 账单云函数和页面已覆盖月份/自定义日期、收支类型、分类、账户、成员、金额区间及日期或金额排序；任一筛选变化都重置到第一页并沿用服务端分页 | 搜索防抖、筛选导出和真机滚动加载压测待补 |
| 2026-08-11 | Phase 4/5 统计与审计契约 | 进行中 | 统计服务端已支持月、年、自定义日期范围和成员/账户/分类筛选的统一聚合口径；操作日志已支持动作分组、操作人名称回填及日期/操作人查询参数 | 统计页范围与维度选择 UI、日志日期/操作人筛选 UI、云端索引和真实数据验收待补 |
| 2026-08-11 | Phase 0 数据边界复核 | 进行中 | 自定义统计范围改为包含结束日全时段；恢复一级分类不会擅自恢复曾单独停用的二级分类；操作日志展示统一为 Asia/Shanghai | 仍需在真实云数据库中用边界日期、停用/恢复组合和多成员日志做回归验收 |
| 2026-08-11 | Phase 3 资料体验 | 进行中 | “我的”页已移除当前版本暂缓且无实际处理逻辑的提醒/关于入口；用户可通过微信头像选择器上传头像至云存储，并经服务端同步到有效成员关系 | 头像文件生命周期、真机授权表现和跨成员即时展示待云端验收 |
| 2026-08-11 | Phase 5 审计分页修正 | 进行中 | 操作日志动作分组改为数据库侧筛选，避免先分页后前端过滤造成“无更多记录”的假象 | 待在真实索引和较大日志量下验证查询性能 |
| 2026-08-11 | Phase 4 当前月未来账单修正 | 进行中 | 服务端明确当前月/当前年默认只统计截至今天的账单；仅未来期段、显式自定义未来结束日或调用方明确允许时才统计未来账单 | 待在上海跨日时刻与未来月份真实数据下验证 |
| 2026-08-11 | Phase 3 资料缓存一致性 | 进行中 | 资料页和记账表单优先读取最新全局用户资料，昵称/头像修改后不会被初始化 Promise 的旧返回值覆盖 | 仍需真机确认头像上传后跨页刷新与成员列表同步 |
| 2026-08-11 | Phase 4 统计范围入口 | 进行中 | 统计页已接入按月、按年和自定义日期范围切换，复用服务端统计口径；月模式保留连续月份导航 | 成员/账户/分类维度筛选 UI、ECharts 图表和真机可读性验证待完成 |
| 2026-08-11 | Phase 3 启动邀请优先级 | 进行中 | 启动参数中的邀请码会保留为待处理邀请；服务端确认有效后优先展示账本、管理员和成员数确认卡，不会先自动创建默认账本；确认加入后才切换当前账本 | 待支持多个待处理邀请、分享链接生成和真实小程序启动参数验收 |
| 2026-08-11 | Phase 3 邀请保留修正 | 进行中 | 待处理邀请的“暂不加入”不再清空邀请码或触发默认账本创建，用户可在后续启动继续确认 | 多邀请列表、明确拒绝/删除动作仍待实现 |
| 2026-08-11 | Phase 3/4 交互补齐 | 进行中 | 管理员可主动撤销当前有效邀请码并写入审计日志；搜索页已加入 350ms 输入防抖，减少连续输入的云函数请求 | 邀请分享链接、多人待处理邀请与搜索大数据索引仍待完成 |
| 2026-08-11 | Phase 0 云端契约复核 | 进行中 | 修复自定义统计范围判断的正则转义，保证显式未来结束日期按约定纳入统计 | 导入回滚资源清理和部署后真实统计边界验证待完成 |
| 2026-08-11 | Phase 5 导入批次清理 | 进行中 | 导入自动创建的分类/账户会标记批次来源；撤销时软删除该批次全部账单，并仅物理清理未被其他有效账单使用的批次资源 | 仍需真实 xlsx、多批次交叉使用与云端批量数据回归验证 |
| 2026-08-11 | Phase 3/5 预算与导出补齐 | 进行中 | 预算页补齐未设置、80% 预警、100% 超支和删除预算状态；管理员可从账单页导出当前筛选结果，服务端按当前筛选条件生成 xlsx | 云存储上传语义、Excel 兼容性及真机文件打开流程待实测 |
| 2026-08-11 | Phase 0 页面状态防护 | 进行中 | 修正首页样式语法问题；家庭管理切换账本改为先通过服务端成员校验，再写入当前账本缓存，失败不会污染本地状态 | 仍需微信开发者工具完成 WXSS/WXML 编译和切换失败场景实测 |
| 2026-08-11 | Phase 3 待处理邀请页面保护 | 进行中 | 个人页、账单详情和记账表单在待处理邀请导致无当前账本时改为明确提示，不再访问空账本对象或继续写入 | 待在真实分享启动后逐页跳转验收 |
| 2026-08-11 | Phase 5 导入成员预览 | 进行中 | 导入预览会展示文件成员到当前账本成员的映射；未匹配成员明确提示将归入管理员，再由管理员确认导入 | 交互式手工重映射、整批事务与真实文件验收待完成 |
| 2026-08-11 | Phase 3 未来预算 | 进行中 | 预算页新增月份前后切换，可提前设置、查看和删除未来月份预算，仍按账本与月份隔离保存 | 真机检查月份切换、未来预算统计显示与安全区适配待完成 |
| 2026-08-11 | Phase 4 统计维度筛选 | 进行中 | 统计页已接入成员、账户、分类筛选，所有条件直接传入服务端聚合并随账本切换重新加载选项 | ECharts 饼图/折线图接入、筛选真机可读性和大数据量统计性能待验证 |
| 2026-08-11 | Phase 6 部署验收准备 | 进行中 | 新增《云开发部署与验收清单》，覆盖集合权限、建议索引、部署顺序、双账号业务验收和真机视觉门槛 | 云端部署、索引创建和双账号真机验收仍未执行，不能标记 MVP 完成 |
| 2026-08-11 | Phase 0 云函数启动优化 | 进行中 | `ledgerFunctions` 与 `accountingFunctions` 的集合检查在同一热实例内只执行一次，减少每个业务请求重复调用集合创建 API | 初次部署集合初始化行为仍需在云环境观察日志确认 |
| 2026-08-11 | Phase 3 邀请重入权限修正 | 进行中 | 已禁止被移除/退出成员使用其离开前生成的邀请码恢复成员关系；只有管理员在其离开后重新生成的邀请码才可使其以普通成员身份重新加入。已是有效成员重复确认仍保持幂等且不改变角色 | 多待处理邀请的独立列表、拒绝/删除入口及真实分享启动参数仍待实现与验收 |
| 2026-08-11 | Phase 2 连续记账与时间字段 | 进行中 | 记账表单补齐日期与时间分别选择；新增账单成功后可选择“再记一笔”，保留收支类型、分类、账户和成员，仅清空金额、商家、备注并重置当前上海时间 | 需在真机确认键盘收起、日期/时间选择器和连续提交防重行为 |
| 2026-08-11 | Phase 5 审计筛选完善 | 进行中 | 操作日志页面已接入操作人、开始日期和结束日期筛选；服务端展示可回填已退出成员的昵称，避免错误显示为“已注销用户” | 注销流程尚未实现；真实云端索引与较大日志量性能待验证 |
| 2026-08-11 | Phase 3 管理接口最小权限 | 进行中 | 全量分类管理接口改为管理员权限，普通成员继续只能读取有效分类以供记账 | 需以普通成员账号进行云端绕过调用验证 |
| 2026-08-11 | Phase 3 多待处理邀请 | 进行中 | 启动分享邀请码已改为本地去重队列，用户可逐条确认、暂不加入或删除；失效邀请码会提示并移除后继续验证下一条，队列未清空前不会创建默认账本 | 需在真实小程序冷启动、热启动与多分享链接场景下验证队列持久化和提示时机 |
| 2026-08-11 | Phase 4 ECharts 运行库与真实图表 | 进行中 | 已纳入官方 `echarts-for-weixin` 的 BSD 许可 `ec-canvas` 运行组件；统计页已接入基于真实服务端数据的分类环形饼图和收支趋势折线图，筛选与收支切换会同步更新图表，排行列表保留为可扫描补充 | 必须在微信开发者工具与真机确认 Canvas 非空、尺寸稳定、滚动/触控无异常和基础库兼容性，未完成前不能判定图表验收通过 |
| 2026-08-11 | Phase 3 邀请分享入口 | 进行中 | 管理员生成邀请码后可用微信分享按钮发送；分享路径只携带邀请码，不暴露 `familyId`，撤销邀请码会立即隐藏对应分享入口 | 真机需验证分享卡片启动参数、队列接收、撤销后的旧链接失效和多次生成的旧码失效 |
| 2026-08-11 | Phase 3 账号注销与历史脱敏 | 进行中 | 已实现服务端注销预检、管理员账本转让/单人成员解散约束、二次确认注销、成员访问撤销、个人资料和记账偏好清理；历史账单在列表、详情、搜索和导出中统一显示为“已注销用户”，不再暴露昵称或头像 | 需在云端以管理员、普通成员和历史账单场景完成回归；解散账本与注销之间的网络中断恢复需实测 |
| 2026-08-11 | Phase 0/3 解散账本访问收敛 | 进行中 | 两个业务云函数的成员校验均确认账本未解散；解散账本的邀请码会明确失效；注销成员状态按顺序、可重试地处理，降低网络瞬断造成的部分完成风险 | 仍需在云端模拟解散后旧缓存请求、注销中断后重试和审计记录完整性 |
| 2026-08-11 | Phase 3 注销流程连续性 | 进行中 | 用户同时管理多个仅本人账本时，界面会在一次明确确认后逐个解散，再继续执行注销；服务端先撤销成员访问后写注销审计，重试不会为已经完成的成员重复记一条注销日志 | 云端仍需验证某个解散步骤失败后的再次发起行为、日志顺序与用户提示 |
| 2026-08-11 | Phase 3 解散重试保护 | 进行中 | 解散动作允许在成员已标记解散但账本状态尚未更新的瞬断窗口继续完成，已完成的解散请求幂等返回，不把半完成账本留在可访问状态 | 仍需在云函数测试环境人为制造中断并核对审计日志仅写入一次 |
| 2026-08-11 | Phase 0 静态质量门禁 | 进行中 | 全量小程序页面、云函数和 ec-canvas JavaScript 语法检查通过；git diff --check 通过；前端未发现直接访问云数据库的调用 | WXML/WXSS 编译、Canvas 实际渲染、云端部署、索引和双账号/真机验收仍未执行 |
| 2026-08-11 | Phase 4/5 搜索与导入边界修复 | 进行中 | 搜索改为扫描当前账本全部未删除账单后再返回匹配结果，避免历史账单被最近 100 条截断；导入支持 XLSX Date、Excel 序列日期和常见日期文本并统一为上海时间格式 | 仍需在云端大账本压测搜索耗时，并用真实随手记文件验证日期、成员映射和重复检测 |
| 2026-08-11 | Phase 5 导入成员安全映射 | 进行中 | 导入仅在成员昵称唯一且规范化后精确匹配；同名昵称或未匹配名称不会静默归到错误成员，而是预览中明确提示并归入管理员 | 仍需真实双成员账号验证重复昵称、导入预览确认和批次结果 |
| 2026-08-11 | Phase 0 数据边界与审计补强 | 进行中 | 修复 Excel 序列日期导入的时区偏移；禁止管理员把管理员权限转给自己；操作日志家庭分组纳入解散与账号注销动作 | 仍需真实 XLSX 边界日期、管理员自转让绕过调用和日志筛选云端验证 |
| 2026-08-11 | Phase 3 邀请信息最小暴露 | 进行中 | 邀请确认接口不再向客户端返回 familyId，仅返回账本名称、管理员昵称、成员数和确认所需状态；正式加入仍由服务端根据邀请码解析账本 | 仍需真机检查分享启动参数、旧链接失效和日志中邀请码脱敏 |
| 2026-08-11 | Phase 6 主包体积与预览编译 | 进行中 | 微信开发者工具首次预览发现源码 2310KB 超过 2MB；排除无引用云开发模板图片后，CLI preview 构建成功，实测主包 1,217,938 Byte（约 1.2MB），保留约 0.78MB 空间给审核后的品牌资源 | 预览编译已通过；仍需真机确认 ECharts Canvas、品牌资源加入后的最终包体与交互表现 |
| 2026-08-11 | Phase 6 运行时错误修复 | 进行中 | 开发者工具运行时检查发现缺失品牌图产生 500、for-of 编译依赖缺少 Babel runtime；图片未落地时改为不请求路径并使用品牌文字占位，批量解散改为 Promise 顺序链 | 需重新编译并清空控制台确认两类错误消失；ImageGen 资源落地后再开启真实图片引用 |

## 7. 部署记录

| 日期 | 云函数 | 版本 | 是否上传部署 | 验证结果 |
|---|---|---|---|---|
| - | - | - | - | - |

## 2026-08-12 进度记录：成员关系与账单写操作事务化加固

> 本次更新为静态代码加固，仅通过语法检查和静态安全扫描，未执行云端部署、双账号联调或真机验收，不标记 MVP 完成。

### 已完成

**Phase 0 成员身份与并发安全**

- `ensureUser()` 改为确定性文档 ID（`getStableUserId`）。所有并发登录最终写入同一确定性用户记录，遗留随机 ID 文档在合并后清理；`getUser()` 先按确定性 ID 读取，再回退到 openid 查询。
- `getFamilyMembers()` 和 `getActiveMember()` 按 openid 去重，使用 `compareMembership` 统一选择规范成员记录（优先 active → admin → 确定性 ID → 最早加入）。
- `getValidMemberships()` 使用分页查询 `getMembershipsByOpenid()` 避免默认 20 条截断，并按 familyId 收敛为规范成员关系。

**Phase 3 成员事务与上限保护**

- `confirmJoinFamily()` 整体迁入 `db.runTransaction`：事务内重新读取并校验邀请码状态/过期、账本未解散、50 人唯一 openid 上限；遗留重复 active 成员收敛为单条并标记其余为 `superseded`；已注销成员重新加入使用确定性 rejoin ID 避免复用旧 cancelled 文档。
- `removeMember` / `leaveFamily` / `transferAdmin` 全部改为事务内重读 family + 成员角色，保证唯一管理员、防自删/自转，并在事务内更新 family 的 `activeMemberCount` 和 `membershipRevision`。
- `dissolveFamily` 改为幂等事务：事务内撤销邀请、将全部 active 成员标记 `dissolved`、更新 family 状态并写一条去重审计日志；中断重试可收敛为 `families.status=dissolved`。
- `cancelAccount` 审计日志改为确定性文档 ID（`getStableDocumentId("account-cancel-log", familyId, memberId)`），网络中断重试不重复写同一条注销记录。

**Phase 2 账单写操作事务化**

- `createBill` / `updateBill` / `deleteBill` 改为事务内校验家庭状态、成员权限、版本号和分类/账户引用，审计日志同事务写入（`addOperationLogInTransaction`），避免"先读后写"窗口导致两个并发编辑都成功。
- `validateBillReferencesInTransaction()` 在事务内重新查询分类/账户，确保引用有效且启用。
- 版本冲突在事务内重读 bill.version，`Number(event.version) !== Number(bill.version)` 直接拒绝覆盖。
- 普通成员编辑时成员归属变更限制以服务端事务查询为准，不再依赖客户端传入的 `creatorOpenId`。

**Phase 0 默认数据初始化修复**

- `initFamilyCategoriesAndAccounts()` 完成后写入 `seedVersion = 1` 标记。
- `ensureFamilySeedData()` 对无 `seedVersion` 标记的遗留账本先检查分类/账户是否完整缺失再补种；已有数据的不重置，只补充缺失的默认项；有标记但版本低的账本执行逐项幂等补种。
- `ensureInitialFamilyInvite()` 用 `initialInviteInitialized` 标记保证首张邀请码创建幂等；管理员手动撤销后不会被登录流程反复重建。
- 默认账本初始化锁的失败重试改为复用同一确定性 familyId 修复未完成步骤，仅在账本已解散或管理员不匹配时才创建新尝试。

### 静态验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（9 步：语法/不直连库/openid 残留/WXML 配平/WXML 规范 wx:key+处理器/视觉令牌一致/action↔handler 契约/主包<2MB/git 空白） | 通过 |
| 单元+契约测试 `npm test`（46 项：纯逻辑 + accountingFunctions 权限/版本冲突/隔离 + ledgerFunctions 邀请/成员上限/管理员转让/初始化幂等 + 导入 preview/confirm/rollback） | 通过 |
| `node --check` 覆盖 `miniprogram/` + `cloudfunctions/` 全部 JS | 通过 |
| 前端无 `wx.cloud.database` 直连 | 通过 |
| 前端 WXML/JS 无 openid/creatorOpenId/memberOpenid/operatorOpenId/data-openid 残留 | 通过 |
| `git diff --check` 空白错误 | 通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 事务 where 更新语义 | 使用 `transaction.collection().where().update()` 批量更新邀请码/成员状态，需在云开发环境确认 SDK 支持且原子性符合预期 | 部署后用并发请求验证；如不支持则改为逐文档更新 |
| 云端联调未执行 | 所有事务/并发/权限逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后立即执行部署清单双账号验收 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；`utils/brand.js` 清单与各页插图/降级已就绪，`available=false`；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true`，逐项做 Alpha/尺寸/DPR/真机验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认 Canvas 初始化、图表非空和触控 | 部署后用开发者工具和真机逐项检查 |
| 真机视觉未验证 | 未覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 需在品牌资源落地后做完整真机走查 |
| 主包体积 | 已删除 23 个无引用模板图，主包实测约 1.15MB（<2MB，门禁已加入 verify.js）；加入品牌资源后必须重新确认仍 < 2MB | 品牌资源落地后重新编译复测 |
| 导入原子性 | 导入仍按行写入，无整批次回滚补偿 | 后续补批次级清理语义并用真实 XLSX 验证 |
| 旧云函数未清理 | `billFunctions`、`initFunctions`、`quickstartFunctions` 为旧模板代码，无前端路径调用 | 部署清单明确仅 `ledgerFunctions` 和 `accountingFunctions` 为生产函数 |

---

## 2026-08-13 进度记录：账单/统计/搜索/导出契约测试补强

> 本次更新为本地静态与契约测试补强，不涉及云端部署、双账号联调或真机验收，不标记 MVP 完成。

### 已完成

**Phase 4/5/6 契约测试补强（内存假数据库）**

- `listBills`：新增默认按日期倒序 + `limit/offset` 分页边界测试；跨账本非成员读取被拒测试。
- `getStats`：新增月度聚合测试（支出/收入/结余与分类排行口径正确）；当前月默认排除未来账单测试。
- `searchBills`：新增覆盖全部未删除历史账单（含早于最近 100 条的记录）并匹配备注/商家测试。
- `exportBills`：新增仅管理员可导出、生成短时下载链接、类型筛选计数测试；为 `exportBills` 补充 `cloud.uploadFile`/`getTempFileURL` 桩与 `xlsx.utils` 桩。
- 预算：新增 `saveBudget`（仅管理员、金额转分）、`getBudget`（读取与删除后置空）、`deleteBudget` 测试。
- `listOperationLogs`：新增仅管理员可读测试。

**测试基建增强**

- `scripts/fake-db.js` 的 `command` 补上 `.and()` 链式方法，精确模拟云开发 SDK 的查询命令对象（`db.command.gte(x).and(...)`），使 `getStats` 等服务端日期范围查询可被本地契约测试覆盖。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（9 步） | 通过 |
| 单元+契约测试 `npm test`（从 46 项增至 **55 项**） | 通过 |
| 新增 accountingFunctions 契约测试 | 19 项（含本次新增 9 项）全部通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端联调未执行 | 分页/聚合/导出/预算逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后按部署清单双账号验收 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认初始化与触控 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 未覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 品牌资源落地后做完整真机走查 |
| 导入原子性 | 导入仍按行写入，无整批次回滚补偿 | 后续补批次级清理语义并用真实 XLSX 验证 |

---

## 2026-08-13 进度记录：解散账本事务重构与契约测试补强

> 本次更新为本地静态与契约测试补强，不涉及云端部署、双账号联调或真机验收，不标记 MVP 完成。

### 已完成

**Phase 0/3 解散账本事务语义收敛（消除「事务 where 更新」风险）**

- `ledgerFunctions` 的 `dissolveFamily` 不再使用 `transaction.collection().where().update()` 批量更新邀请码与成员状态（该写法在云开发事务 SDK 中支持性不确定），改为事务内先按 `where().limit().get()` 查询出待撤销邀请与待解散成员，再对每个文档用 `transaction.collection().doc(id).update()` 逐条更新。
- 撤销邀请、标记成员 `dissolved`、更新账本状态与去重审计日志仍保持在同一个 `runTransaction` 内，保证「账本状态已解散则成员不可访问、邀请码已失效」的原子收敛语义不变。

**Phase 3 解散契约测试补强**

- 新增 3 项 `dissolveFamily` 契约测试：仅管理员且无其他成员时可解散并收敛 `families.status=dissolved`、成员 `dissolved`、邀请码 `revoked`；有其他成员时被拒并提示先转让；已解散账本重复解散幂等返回 `alreadyDissolved`。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（9 步） | 通过 |
| 单元+契约测试 `npm test`（从 55 项增至 **58 项**） | 通过 |
| ledgerFunctions 契约测试（新增解散收敛/拒转让/幂等 3 项） | 12 项全部通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后按部署清单双账号验收 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认初始化与触控 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 未覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 品牌资源落地后做完整真机走查 |
| 导入原子性 | 导入逐行写入，`rollbackImport` 已提供整批次补偿并清理自动创建的分类/账户，但 `confirmImport` 中途失败时前端可能拿不到 batchId 触发回滚 | 后续为中途失败保留 batchId 返回路径，并用真实 XLSX 验证 |

---

## 2026-08-13 进度记录：导入中途失败可回滚（原子性补强）

> 本次更新为本地静态与契约测试补强，不涉及云端部署、双账号联调或真机验收，不标记 MVP 完成。

### 已完成

**Phase 5 导入中途失败可回滚**

- `accountingFunctions` 的 `confirmImport` 将逐行写入循环包入 try/catch：若中途发生数据库错误，把 `batchId` 与已导入数 `imported` 附加到抛出的错误上再重抛；`exports.main` 的错误分支将 `batchId`/`imported` 透传到失败响应。
- 前端 `pages/import/index.js` 的 `call` 透传失败响应中的 `batchId`/`imported`；`confirmImport` 捕获到带 `batchId` 的错误时保留 `lastBatchId` 并提示「已导入 N 条后中断，可撤销本次已导入的账单」，使页面「撤销刚刚导入」按钮可用。
- 新增契约测试：模拟第 2 条账单写入时数据库中断，验证失败响应保留 `batchId` 与 `imported=1`、仅写入成功那条、且该半导入批次可被 `rollbackImport` 撤销。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（9 步） | 通过 |
| 单元+契约测试 `npm test`（从 58 项增至 **59 项**） | 通过 |
| 导入契约测试（新增中途失败回滚 1 项） | 7 项全部通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务/导入中断逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后按部署清单双账号验收，并用真实随手记 xlsx 验证导入中断与回滚 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认初始化与触控 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 未覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 品牌资源落地后做完整真机走查 |

---

## 2026-08-13 进度记录：旧云函数清理（消除误部署风险）

> 本次更新为项目清理，不涉及云端部署、双账号联调或真机验收，不标记 MVP 完成。

### 已完成

**Phase 6 旧云函数清理**

- 删除 `cloudfunctions/` 下无任何前端引用的旧模板/历史云函数：`billFunctions`、`initFunctions`、`quickstartFunctions`。
- 已核实前端 `miniprogram/` 全部 JS/WXML 均只调用 `accountingFunctions` 与 `ledgerFunctions`，无上述旧函数引用；`billFunctions/initCategories.js` 的分类种子逻辑已由生产函数的 `ensureFamilySeedData` 承担，删除不丢失功能。
- 现在 `cloudfunctions/` 仅保留生产函数 `accountingFunctions` 与 `ledgerFunctions`，避免在云开发控制台「上传全部云函数」时误部署旧逻辑造成版本/权限冲突。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（9 步） | 通过 |
| 单元+契约测试 `npm test`（59 项） | 通过 |
| 前端 action↔handler 契约一致性（46 个前端 action） | 通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务/导入中断逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后按部署清单双账号验收，并用真实随手记 xlsx 验证导入中断与回滚 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认初始化与触控 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 未覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 品牌资源落地后做完整真机走查 |

---

## 2026-08-13 进度记录：真机安全区适配（记账页固定底栏）

> 本次更新为前端静态改进，未在真机验证，不标记 MVP 完成。

### 已完成

**Phase 1/6 真机安全区适配**

- 盘点全部页面/组件的固定底栏：仅 `pages/index/index`（含 `env(safe-area-inset-bottom)`）与 `pages/addBill/index`（缺失）存在 `bottom: 0` 固定元素。
- 为 `pages/addBill/index.wxss` 的固定保存按钮 `.save-btn` 补充 `env(safe-area-inset-bottom)` 高度与底部内边距（改为 flex 垂直居中），避免 iPhone 刘海屏/Home 指示条遮挡主操作按钮。
- 为分类选择器的底部弹层 `.picker-content` 补充 `env(safe-area-inset-bottom)` 内边距，避免内容被 Home 指示条遮挡。
- 与 `index.wxss` 的既有写法保持一致，未新增其他页面/组件（经扫描无其他固定底栏）。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（9 步） | 通过 |
| 单元+契约测试 `npm test`（59 项） | 通过 |
| 固定底栏安全区覆盖（addBill 保存按钮与底部弹层） | 已补 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务/导入中断逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后按部署清单双账号验收，并用真实随手记 xlsx 验证导入中断与回滚 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认初始化与触控 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 安全区适配为静态改进，仍需在 iPhone 刘海屏与小屏 Android 真机确认键盘顶起、长文本布局和底部按钮不被遮挡 | 品牌资源落地后做完整真机走查 |

---

## 2026-08-13 进度记录：账单页下拉刷新修复 + 静态门禁

> 本次为前端静态修复与验收门禁补强，不涉及云函数变更与云端部署，不标记 MVP 完成。

### 已完成

**Phase 4 账单页下拉刷新生效**

- `pages/bills/index.js` 已实现 `onPullDownRefresh`（重置 offset 后重新加载、`wx.stopPullDownRefresh`），但 `pages/bills/index.json` 缺少 `enablePullDownRefresh: true`，导致下拉手势不会触发处理器，功能实际失效（首页、日志页均已正确配置，仅账单页遗漏）。
- 已在 `pages/bills/index.json` 补齐 `enablePullDownRefresh: true`，使基线 §12「账单列表支持下拉刷新」真正生效。

**Phase 6 静态门禁补强**

- `scripts/verify.js` 新增第 6 步「页面下拉刷新配置配对」：自动扫描每个页面，`onPullDownRefresh` 处理器与 `index.json` 的 `enablePullDownRefresh` 必须成对（有处理器无配置、有配置无处理器均报错并列出页面），防止同类遗漏回归。
- 一键静态验收 `npm run verify` 由 9 步增至 **10 步**。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（10 步，含下拉刷新配置配对门禁） | 通过 |
| 单元+契约测试 `npm test`（59 项） | 通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务/导入中断逻辑仅静态推理，未在真实云数据库环境下验证 | 部署后按部署清单双账号验收，并用真实随手记 xlsx 验证导入中断与回滚 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具与 Hermes 后端本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，但未在开发者工具或真机确认初始化与触控 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 下拉刷新需在真机确认手势触发与加载反馈；整体仍需覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 品牌资源落地后做完整真机走查 |

---

## 2026-08-13 进度记录：记账页成员信息缺失自动恢复

> 本次为前端鲁棒性修复与契约测试补强，涉及云函数仅新增测试断言（未改生产逻辑），不标记 MVP 完成。

### 问题与根因分析

- 症状：进入记账页提示「成员信息缺失，请返回首页后重试」，表单无法打开。
- 直接原因：`pages/addBill/index.js` 的 `onLoad` 在 `app.globalData.currentFamily.memberId` 为空时直接抛错，没有任何恢复路径；该字段是 `createBill` 服务端必填项，缺失即死路。
- 排查过程：用内存假数据库对 `initUser`（首次/幂等/多用户）、`confirmJoinFamily`（老成员/新成员）、`getFamilyDetail`（切换账本）、`createFamily`/`createDefaultFamily` 全场景复现，当前工作区云函数**全部正常返回 `family.memberId`**；前端所有写入 `globalData.currentFamily` 的路径（app 初始化、确认邀请、切换账本、家庭页详情）也均含 `memberId`。
- 结论：工作区代码本身无缺陷，症状最可能来自**云端部署的旧版本云函数**（返回的 `family` 结构不含 `memberId`）或全局状态陈旧；前端硬失败放大了问题。

### 已完成

**Phase 2 记账页成员信息自动恢复**

- `pages/addBill/index.js` 新增 `resolveCurrentMember()` 恢复链路：`currentFamily.memberId` 缺失时先调用 `getFamilyDetail` 重新拉取账本详情（返回 `family.memberId` 并刷新全局缓存）；仍缺失则重置 `initializePromise` 重新初始化；全部失败才提示，避免直接死路。

**Phase 6 契约测试锁定 memberId**

- `scripts/test-init.js`：3 项断言升级——首次/幂等/多用户 initUser 必须返回 `family.memberId`。
- `scripts/test-ledger-contract.js`：confirmJoinFamily（老成员幂等、新成员加入）断言 `family.memberId`；新增 1 项「getFamilyDetail 返回 family.memberId」契约测试（前端记账/切换账本依赖）。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 单元+契约测试 `npm test`（从 59 项增至 **60 项**） | 通过 |
| 一键静态验收 `npm run verify`（10 步） | 通过 |
| `node --check` addBill 语法 | 通过 |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端部署版本未知 | 若云端 `ledgerFunctions` 为旧版本（`family` 不含 `memberId`），前端兜底仍无法恢复 | **需在微信开发者工具中右键重新「上传并部署」`ledgerFunctions`（与 `accountingFunctions`）**，当前工作区版本已验证正确 |
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务/导入中断逻辑仅静态推理 | 部署后按部署清单双账号验收，并用真实随手记 xlsx 验证导入 |
| 品牌资源缺失 | `miniprogram/images/brand/` 仍为空；ImageGen 工具本会话不可用 | 资源生成后置 `available=true` 并逐项验证 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，未在开发者工具或真机确认 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 整体仍需覆盖刘海屏、小屏 Android、键盘顶起、长文本布局 | 品牌资源落地后做完整真机走查 |

---

## 2026-08-14 进度记录：Phase 1 品牌资源全部落地

> 本次将品牌 Logo 与 6 张插画资源全部生成、抠图、压缩并落地到 `miniprogram/images/brand/`，同时重平衡主包体积，静态验收与测试全绿。仍不标记 MVP 完成（云端部署/双账号联调/真机视觉等外部项未消解）。

### 已完成

**Phase 1 品牌资源生成与验收**

- 全部 7 个品牌资源已生成并落地 `miniprogram/images/brand/`，文件名与 `brand.js` 引用一致：
  - `bun-logo.png`（导航 Logo，横版：包子 + 文字，透明底）
  - `empty-bills.png`（首页无账单空态：包子 + 空收据）
  - `welcome-guide.png`（欢迎引导：挥手包子 + 书本 + 金币）
  - `search-empty.png`（搜索无结果：包子 + 放大镜 + 问号）
  - `import-empty.png`（导入空态：包子 + 文件 + 上传箭头）
  - `import-done.png`（导入完成：微笑包子 + 对勾 + 金币）
  - `network-error.png`（网络异常：担忧包子 + 带斜杠云）
- 生成方式：ImageGen（agnes-image-2.1-flash）纯色底生成 → 抠图转透明（RGBA，Alpha 校验 opaque 占比合理）→ 裁边 → 缩放。
- 逐张经本地视觉识别验收：主体清晰、无缺笔画、无边框、无多余文字、风格统一（暖橙/奶黄、棕色卡通包子）。

**Phase 6 主包体积重平衡**

- 品牌资源加入后主包一度达 2452KB 超 2MB 上限；插画统一压到 400px、Logo 压到 600px 宽后，主包回落至 **2002KB < 2MB**（verify 第 9 步通过）。

### 验证结果

| 检查项 | 结果 |
|---|---|
| 一键静态验收 `npm run verify`（**12 步**，含「品牌资源完整性」「静态图片路径有效性」门禁，主包 <2MB） | 通过（主包 2002KB，7 张品牌资源均存在且透明，WXML 图片与 tabBar 图标路径均有效） |
| 单元+契约测试 `npm test`（60 项） | 通过 |
| 品牌资源 Alpha/尺寸/体积核对 | 7 张均 RGBA 透明，共约 816KB |

### 当前风险（未消解）

| 风险 | 说明 | 建议 |
|---|---|---|
| 云端部署版本未知 | 若云端 `ledgerFunctions` 为旧版本（`family` 不含 `memberId`），前端兜底仍无法恢复 | **需在微信开发者工具中右键重新「上传并部署」`ledgerFunctions`（与 `accountingFunctions`）** |
| 云端联调未执行 | 分页/聚合/导出/预算/解散事务/导入中断逻辑仅静态推理 | 部署后按部署清单双账号验收，并用真实随手记 xlsx 验证导入 |
| ECharts Canvas 未验证 | 统计页已接入 ec-canvas，未在开发者工具或真机确认 | 部署后用开发者工具和真机检查 |
| 真机视觉未验证 | 品牌插画与 Logo 需在真机确认清晰度、裁切与安全区；覆盖刘海屏、小屏 Android、键盘顶起 | 品牌资源落地后做完整真机走查 |
