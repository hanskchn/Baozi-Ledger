# 家庭记账本项目需求基线

> 本文档记录用户已确认的全部业务规则，是后续所有开发任务、产品决策和 AI 协作的最高优先级执行依据。

## 1. 执行优先级

本文件与 `需求文档.md` 同时有效，冲突时按以下顺序处理：

1. 用户最新明确确认的规则（以本基线为准）
2. 本需求基线
3. `需求文档.md`
4. 现有代码实现

发现规则矛盾时，先向用户报告并暂停相关实现，不得擅自选择。"后续考虑"功能不属于当前版本实现范围，除非用户重新确认。

## 2. 技术和安全原则

- 微信小程序原生开发（WXML + WXSS + JS），微信云开发（云函数 Node.js + 云数据库 + 云存储）
- 前端绝不直接读写云数据库，所有业务数据必须经过云函数
- 云函数通过 `cloud.getWXContext()` 获取当前用户身份
- 所有账本操作必须校验当前用户是否为有效成员
- 写操作必须校验角色权限（admin/member）
- 普通成员只能操作自己创建的账单
- 管理员可操作当前账本全部账单和账本设置
- 所有重要写操作必须记录操作日志
- 初始化、补齐默认数据和邀请加入必须幂等，不能重复创建
- 云端校验不能只依赖前端校验
- 所有时间统一按中国时区 `Asia/Shanghai` 处理
- 数据库存储标准时间戳，展示和统计时转换为中国时区

## 3. 首次登录和默认账本

首次打开小程序时自动发起微信登录。登录成功后按以下优先级处理：

1. **有效邀请**：先验证邀请 → 用户确认加入 → 直接加入邀请对应账本 → 不创建默认账本
2. **有效待处理邀请**：展示待处理邀请 → 用户可逐个确认、拒绝或删除 → 有有效待处理邀请时不自动创建默认账本
3. **已有可用账本**：进入上次选择且仍有权限的账本
4. **无可用账本且无有效邀请**：自动创建默认账本

### 默认账本规则

- 默认名称：`我的家庭账本`
- 若当前用户已有同名账本，依次使用 `我的家庭账本 2`、`我的家庭账本 3`……
- 当前用户成为管理员和第一个成员
- 自动初始化默认分类（见第 9 节）
- 自动初始化默认账户（见第 10 节）
- 不自动创建预算
- 自动生成有效期 7 天的邀请码（不自动弹出分享）
- 初始化完成后自动设为当前账本
- 首次创建完成后显示一次欢迎引导（可跳过）
- 初始化失败时不进入可记账首页，支持重试和下次补齐
- 已创建的数据不重复创建
- 登录失败或用户暂时拒绝授权：不创建账本、不创建成员关系、不写入业务数据，允许稍后重试

## 4. 用户资料

- 优先使用微信授权获得的昵称和头像
- 拒绝授权时昵称用 `微信用户`，头像用默认头像
- 用户可在"我的"页面修改昵称和头像
- 昵称和头像修改后，在成员列表、账单详情和统计中同步更新
- 不改变账单归属、创建者和权限
- 同一账本允许成员使用相同昵称，通过头像/角色/编号区分
- 成员关系以 `openid` 为唯一标识，不以昵称判断
- 用户不能修改其他成员资料

## 5. 账本和多账本

- 一个用户可以创建或加入多个账本
- 所有业务页面（首页、账单、统计、预算、分类、账户）只显示当前账本数据
- 不混合多个账本的数据
- 用户切换账本后，所有业务页面同步切换
- 用户手动切换后保存当前账本
- 下次打开优先进入上次选择且仍有权限的账本
- 上次账本无权限时，自动选择其他可用账本
- 没有可用账本时按新用户规则处理
- 新加入的账本立即设为当前账本
- 每个账本最多 50 名成员（管理员计入人数）
- 每个账本只能有一名管理员
- 创建者初始为管理员
- 管理员转让后，原管理员变为普通成员
- 只有管理员可以修改账本名称、管理成员、管理分类、管理账户、设置预算、管理邀请码、导入导出数据

### 账本名称规则

- 不能为空，长度 1～30 个字
- 忽略首尾空格
- 英文按不区分大小写比较
- 用户自己创建的新账本不能与已有或已加入账本重名
- 用户可以加入名称与已有账本重复的共享账本（共享账本保留管理员设置的名称，列表通过"账本名称 + 管理员昵称"区分）
- 管理员改名后立即同步给所有成员，写入操作记录

## 6. 邀请加入

- 只有管理员可以生成、撤销和分享邀请码
- 邀请码默认有效期 7 天
- 一个邀请码在有效期内可被多个用户使用
- 同一用户重复点击不会重复创建成员关系
- 已是成员时：不改变角色，直接切换到该账本，提示"你已在该账本中"
- 管理员重新生成邀请码时，旧邀请码立即失效
- 管理员可手动撤销邀请码，撤销后不自动生成新邀请码
- 邀请卡片显示：账本名称、管理员昵称、"加入家庭账本"文案
- 分享链接只携带邀请码，不暴露 `familyId`
- 页面和日志不完整暴露邀请码
- 用户点击邀请卡片后必须先登录
- 服务端验证邀请码（是否存在/过期/被撤销/所属账本/成员数是否已达 50）
- 验证通过后显示确认页（账本名称、管理员昵称、成员数量、加入后可查看的数据范围）
- 用户点击确认后才正式加入
- 用户取消加入：不加入账本，不创建默认账本，保留为待处理邀请
- 多个邀请可同时保留，用户逐个处理
- 无效/过期/撤销的邀请：不加入账本，不创建默认账本，显示明确错误提示，之后按无账本规则初始化
- 被邀请用户加入后：成为普通成员，使用原账本全部已有数据，不复制数据，不显示创建默认账本引导
- 已被移除的用户再次登录按新用户规则处理；如果收到同一账本的新邀请可重新加入

## 7. 成员退出、移除、管理员转让和注销

### 普通成员退出
- 可在家庭管理中主动退出
- 退出后立即失去访问权限
- 历史账单和操作记录保留
- 账本从列表中完全隐藏
- 退出后无其他可用账本时，自动创建新的 `我的家庭账本`
- 再次登录按新用户规则处理

### 管理员转让
- 只有当前管理员可以发起
- 只能转让给当前账本中的普通成员
- 转让后原管理员变为普通成员
- 新管理员获得全部账本设置权限
- 账本只有管理员一人时不能转让

### 管理员移除成员
- 需要二次确认
- 被移除成员立即失去权限，完全不再看到该账本
- 历史账单保留
- 之后可通过新邀请码重新加入
- 写入操作记录
- 管理员不能移除自己

### 账号注销
- 支持用户注销账号
- 注销前需二次确认
- 管理员必须先处理自己管理的全部账本（有其他成员时转让管理员，只有自己时先解散账本）
- 注销后退出全部账本并移除权限
- 历史账单保留，注销用户显示为"已注销用户"，不显示头像和昵称
- 删除个人资料、登录记录和个人分类/账户记忆
- 注销后再次登录视为新用户

## 8. 默认账户

默认账户（新账本自动初始化）：
- 现金（默认选中）
- 微信
- 支付宝
- 银行卡
- 信用卡
- 其他

### 账户规则
- 第一个默认账户为 `现金`
- 后续记忆用户最近使用的有效账户
- 支出和收入共用最近使用账户
- 同一账本账户名称不能重复
- 名称比较忽略首尾空格，英文不区分大小写
- 已停用账户仍占用原名称
- 不同账本可以有同名账户
- 支持新增、修改、删除、停用
- 未被账单使用的账户可物理删除
- 已被账单使用的账户删除时执行停用
- 停用账户不能用于新账单，历史账单继续显示原名称
- 账户改名后历史账单同步显示新名称
- 每个账本至少保留一个可用账户（最后一个可用账户不能删除或停用）
- 如果记忆账户已失效，回退到 `现金`
- 仅管理员可管理账户

## 9. 默认分类

### 预设分类（新账本自动初始化）

**支出分类：**
- 餐饮（二级：家常菜、外卖、快餐、零食、水果、聚餐、咖啡、其他）
- 交通（二级：公交地铁、打车、停车、加油、共享单车、其他）
- 购物（二级：日用品、服饰、电子产品、食品、美妆、其他）
- 居住（二级：房租、水电煤、物业费、装修、维修、其他）
- 娱乐（二级：影视、游戏、旅游、运动、社交、其他）
- 医疗（二级：药品、体检、挂号、牙科、其他）
- 教育（二级：学费、培训、书籍、资料、其他）
- 人情（二级：红包、送礼、请客、随份子、其他）
- 通讯（二级：话费、网费、会员订阅、其他）
- 其他（二级：其他支出）

**收入分类：**
- 工资（二级：底薪、绩效、加班费、其他）
- 奖金（二级：年终奖、季度奖、项目奖、其他）
- 兼职（二级：临时兼职、长期兼职、其他）
- 理财（二级：利息、基金、股票、其他）
- 红包（二级：收到红包、拼手气、其他）
- 其他（二级：其他收入）

### 分类规则
- 固定一级分类 + 二级分类两级结构
- 每笔账单必须选择到二级分类
- 一级分类至少保留一个可用二级分类
- 一级分类下最后一个可用二级分类不能删除或停用
- 支出和收入可以使用同名分类
- 同一账本、同一收支类型下，一级分类名称不能重复
- 同一一级分类下，二级分类名称不能重复
- 名称比较忽略首尾空格，英文不区分大小写
- 不同一级分类下允许二级分类同名
- 已停用分类仍占用原名称
- 支持新增、修改、删除、停用
- 未被账单使用的分类可物理删除
- 已被账单使用的分类执行软删除/停用，历史账单继续显示
- 一级分类删除时：从未被使用可整体删除；已被使用则整体停用
- 分类改名/改图标后历史账单同步显示最新信息
- 操作记录保留修改前后摘要
- 支出首次默认一级分类为 `餐饮`，收入首次默认一级分类为 `工资`

## 10. 分类和账户记忆

- 记忆按"用户 + 账本"保存
- 支出和收入分别保存最近使用分类
- 支出首次回退到 `餐饮`，收入首次回退到 `工资`
- 账户使用统一的最近使用账户
- 支持跨设备和重新安装后恢复
- 不同账本分别记忆，互不影响
- 已被删除或停用的记忆自动回退到有效默认值

## 11. 账单字段和权限

### 账单字段

| 字段 | 是否必填 | 默认值 | 备注 |
|------|---------|--------|------|
| 类型（支出/收入） | 必填 | 支出 | |
| 金额 | 必填 | 空 | 必须 >0，最多 2 位小数，单笔最大 9,999,999.99 |
| 日期 | 必填 | 今天 | 中国时区 |
| 时间 | 必填 | 当前时间 | 中国时区 |
| 一级分类 | 必填 | 餐饮（支出）/ 工资（收入） | |
| 二级分类 | 必填 | 对应一级下的第一个 | |
| 账户 | 必填 | 现金或最近使用有效账户 | |
| 成员 | 必填 | 当前登录用户 | |
| 商家 | 可选 | 空 | 最多 50 字 |
| 备注 | 可选 | 空 | 最多 200 字 |

> 不再保留独立的"记账人"字段。系统内部仍记录"原始创建者"（用于权限和审计），不在表单中展示。

### 内部记录字段（表单不展示）
- 原始创建者（`creatorOpenId`）
- 创建时间
- 最后修改时间
- 版本号
- 当前账单成员归属

### 权限规则
- 普通成员创建账单时，成员固定为自己
- 管理员创建或编辑账单时，可选择当前有效成员
- 普通成员只能编辑和删除自己创建的账单
- 管理员可编辑和删除所有账单
- 普通成员不能把成员改成其他人
- 管理员可以调整账单成员归属（修改后统计归属同步转移，原创建者不变，权限按原创建者判断）
- 已退出/被移除/已注销成员不能作为新账单成员
- 历史账单保留原成员身份
- 普通成员之间不能直接转让账单归属
- 普通成员和管理员均可随时编辑或删除，暂无时间限制
- 删除必须二次确认，无回收站、不可恢复
- 编辑和删除均需要版本号校验，并发冲突时拒绝覆盖，提示刷新后重试
- 管理员编辑或删除其他成员账单时不发送通知

### 金额校验
- 必须大于 0，最多两位小数，单笔最大 9,999,999.99 元
- 不允许负数、零、科学计数法或非数字字符
- 前端和云函数双重校验
- 支出和收入均保存正数，由类型区分

### 保存和连续记账
- 保存成功后刷新首页、账单列表和统计
- 提示保存成功并返回上一页或首页
- 支持"再记一笔"
- "再记一笔"保留类型、分类、账户和成员，金额、商家、备注清空，日期时间重置为当前时间
- 保存失败时保留当前页面输入内容并支持重试
- 不支持账单草稿

### 账单日期
- 允许过去日期和未来日期
- 列表和统计按账单实际日期计算
- 保留创建时间和最后修改时间
- "今天/本周/本月"按中国时区计算
- 未来账单不计入已发生汇总，但在选定未来日期范围内参与统计

## 12. 账单列表、搜索和统计

### 账单列表
- 默认按实际账单日期倒序
- 每页 20 条
- 首页最近账单加载 10 条
- 支持下拉刷新
- 滚动到底部自动加载下一页
- 搜索和筛选变化时回到第一页
- 无更多数据时显示提示

### 搜索和筛选
- 商家和备注关键词
- 收支类型
- 今天、本周、本月、自定义日期范围
- 一级分类和二级分类
- 账户
- 成员
- 金额区间
- 按日期倒序 / 按金额升序或降序
- 只查询当前账本

### 统计
- 总收入、总支出、结余
- 按月、按年和自定义日期范围
- 支出和收入分类占比（饼图）
- 分类金额排行
- 按天或按月的收支趋势（折线图）
- 按成员统计收入、支出和结余
- 支持按成员、账户、分类筛选
- 未来日期账单仅在选定日期范围包含未来日期时参与统计

## 13. 预算

### 13.1 基本规则

- 预算按"账本 + 月份"保存
- 新账本不自动创建预算
- 未设置预算时显示"尚未设置预算"
- 仅管理员可设置、修改和删除预算
- 预算只统计支出，不扣除收入
- 支出达到 80% 时提示预算预警
- 达到或超过 100% 时提示超支
- 预算只提醒，不限制继续记账
- 可以提前设置未来月份预算
- 修改预算后立即重新计算
- 金额单位为人民币元，输入最多两位小数，范围 0～99,999,999.99

### 13.2 首页预算概览卡片

- 首页（记账 Tab）在金额摘要区域下方展示本月预算卡片，样式精简
- 卡片一行展示：预算 ¥xxx、已支出 ¥xxx、剩余/超支 ¥xxx，下方一条细进度条和百分比
- 未设置本月预算时，卡片显示"设置本月预算 ›"入口，点击跳转到预算页
- 已设置预算时，点击卡片跳转到预算页查看/修改
- 进度条颜色：正常（< 80%）为主题橙色，预警（80%～99%）为橙红色，超支（≥ 100%）为红色
- 卡片只在当前账本有成员身份时显示
- 预算数据合并到首页已有的 getHomeSummary 云函数接口中返回，不新增网络请求

### 13.3 预算页布局

页面从上到下依次为：

1. **月份切换栏**
   - 左箭头 ‹、中间月份文字（可点击）、右箭头 ›
   - 月份显示格式为"YYYY年M月"（如"2026年8月"），不使用"2026-08"格式
   - 当前月份文字旁显示"本月"标签（仅当月显示）
   - 点击月份文字弹出月份选择器（picker mode="date" fields="month"），可快速跳到任意月份（支持跨年）
   - 左右箭头用于相邻月份微调，无年份上限
   - 月份选择器弹层中，已设置预算的月份显示小圆点标记（通过自定义弹层实现，系统 picker 不支持时退化为仅显示当前月份是否有预算的文字提示）

2. **预算概览卡片**
   - 顶部显示"本月预算"标签和金额输入区（管理员）或纯文本金额（普通成员）
   - 金额输入区：人民币符号 ¥ + 数字输入框（type="digit"），字号醒目
   - 输入框 placeholder 为"0.00"
   - 输入时实时校验：禁止非数字和小数点、限制两位小数、限制最大值
   - 金额为 0 时提示"请输入预算金额"，保存按钮置灰

3. **支出统计卡片**
   - 展示已支出、剩余金额、使用比例
     - 已支出（红色）
     - 剩余金额（绿色，超支时变为红色并显示"已超支 ¥xxx"）
     - 使用比例（百分比）
   - 横向进度条，宽度按使用比例填充，最大 100%
   - 进度条下方提示文案：
     - 未设置预算：管理员看到"尚未设置本月预算"（灰色）；普通成员看到"管理员尚未设置本月预算"
     - 已支出比例 < 80%："已使用 xx%"（灰色）
     - 80% ≤ 比例 < 100%："本月预算已使用 xx%，请留意支出"（橙红色）
     - 比例 ≥ 100%："本月支出已超出预算 xx%"（红色）
     - 当前查看的是未来月份且已设预算："预算已设置，将在该月生效"（灰色），已支出显示 ¥0.00

4. **快捷操作区**（仅管理员可见）
   - "复制上月预算"按钮：点击后将当前查看月份的上一个月预算金额填入输入框（不自动保存，需手动点保存）
   - 如果上一个月没有预算，按钮置灰不可点，文案显示"上月未设置预算"
   - "删除本月预算"按钮：有二次确认弹窗

5. **底部保存按钮**（仅管理员可见）
   - 全宽主题色按钮"保存"
   - 金额未变化、为空或为 0 时按钮置灰不可点
   - 保存成功后显示 Toast"已保存"，并立即重新拉取统计数据刷新页面

### 13.4 管理员与普通成员的视图差异

- **管理员**：完整功能，可输入金额、保存、复制上月、删除
- **普通成员**：
  - 预算金额显示为纯文本，不可编辑
  - 不显示输入框、保存按钮、复制上月按钮、删除按钮
  - 支出统计卡片和进度条正常显示
  - 未设置预算时显示"管理员尚未设置本月预算"
  - 首页预算卡片同样可见（只读）
- 权限以后端校验为准，前端只做展示控制

### 13.5 交互规则

- **月份切换时的未保存提示**：
  - 如果当前输入框金额与已保存预算不同（包括从有预算改为空），通过箭头、月份选择器切换月份前弹出确认弹窗："当前预算尚未保存，是否放弃修改？"
  - 点击"放弃"切换月份，点击"继续编辑"留在当前页
  - 金额无变化时直接切换，不弹窗

- **保存逻辑**：
  - 金额必须 > 0 才能保存
  - 保存成功后立即重新调用统计接口刷新已支出数据
  - 保存后按钮恢复置灰状态，直到再次修改金额

- **删除预算**：
  - 二次确认弹窗："删除后本月不再显示预算预警，确定删除？"
  - 删除后输入框清空、进度条归零、提示文案变为"尚未设置本月预算"
  - 首页预算卡片变为"设置本月预算"入口

- **复制上月预算**：
  - "上月"指当前查看月份的上一个月（非自然日历上个月），例如查看 2026年3月 时复制的是 2026年2月 的预算
  - 仅将金额填入输入框，不自动保存
  - 如果上一个月无预算，按钮置灰
  - 不自动追溯更早月份

- **未来月份预算**：
  - 可通过月份选择器或箭头切换到未来任意月份设置预算
  - 未来月份的"已支出"显示为 ¥0.00，进度条为 0%
  - 未来月份提示文案为"预算已设置，将在该月生效"

### 13.6 视觉风格

- 页面背景使用全局暖色系 `#FFF8EC`
- 卡片使用白色圆角卡片，圆角 20rpx，轻阴影
- 主题色 `#FF8C42`，超支色 `#E54545`，正常剩余色 `#4CAF50`
- 金额数字使用加粗字体
- 与首页、账单页、统计页风格保持一致

### 13.7 时区

- 所有月份计算统一使用中国时区 UTC+8
- 禁止使用 `new Date(year, month)` 等本地时区构造方式，避免跨时区偏差

## 14. 通知（均为可选，未授权不影响核心功能）

### 每日记账提醒
- 默认关闭，用户自行开启
- 按中国时区发送，用户可设置提醒时间
- 当天所有仍有权限的账本均无账单时才发送，任一账本有账单则不发送
- 不展示账本名称或账务数据
- 用户退出/被移除/注销后停止

### 预算提醒
- 小程序内始终显示预警和超支状态
- 用户主动授权后才发送微信订阅消息
- 首次达到 80% 和首次达到/超过 100% 各提醒一次
- 默认只通知管理员
- 管理员可选择是否通知普通成员

### 成员记账通知
- 默认关闭，管理员可在家庭管理中开启
- 普通成员新增/编辑/删除账单时，通知已授权管理员
- 通知内容：操作类型、金额、账单日期（不含备注等敏感信息）
- 管理员自己的操作不通知其他成员
- 管理员退出/被移除/注销后停止接收

## 15. 操作记录

仅管理员可见，记录账本级别的操作：

- 成员加入、退出、被移除
- 管理员转让
- 账本改名
- 分类新增/修改/停用/删除
- 账户新增/修改/停用/删除
- 预算设置/修改
- 邀请码生成、撤销、重新生成
- 账单新增、编辑、删除
- 导入操作、导出操作

账单操作记录：
- 新增：操作人、时间、金额、类型、账单标识
- 编辑：操作人、时间、账单标识、发生变化的字段名称（不含具体金额/备注内容）
- 删除：操作人、时间、账单标识、金额、类型
- 不记录图片凭证内容，不保存邀请码明文，不展示 openid/版本号等技术字段

规则：
- 不保存编辑前后的完整账单内容
- 不记录图片凭证内容或邀请码明文
- 默认永久保留，账本解散时一并删除
- 支持按操作类型、操作人和日期范围筛选
- 操作记录不可手动修改或删除

## 16. 数据导入和导出

### 导入（仅管理员可用）
- 兼容随手记 xlsx：支出和收入两个 Sheet
- 不存在的分类和账户自动创建
- 文件中的"成员"优先匹配账本成员，匹配失败归管理员
- 文件中的"记账人"字段忽略
- 自动识别高度相似的重复账单并跳过
- 导入完成显示成功/跳过重复/失败数量
- 每次导入生成批次标识
- 管理员可撤销某次导入批次（只删除该批次导入的账单，不删除原有账单）
- 撤销后清理本批次自动创建且未被其他账单使用的分类/账户
- 已被其他账单使用的分类/账户保留
- 导入和撤销写入操作记录

### 导出（仅管理员可用）
- 支持当前筛选结果或全部账单
- 支持按日期范围导出
- 使用 xlsx 格式
- 只导出当前账本
- 字段：日期、类型、一级分类、二级分类、账户、金额、成员、商家、备注
- 不包含独立的"记账人"字段
- 通过云存储生成临时下载链接，短期有效
- 导出行为写入操作记录
- 普通成员不显示导出入口

## 17. 当前版本暂缓

以下功能已讨论但暂不实现，未经用户重新确认不得提前实现：

- **账单图片凭证**：计划支持每笔最多 3 张（jpg/jpeg/png/webp，压缩后单张 ≤2MB），云函数鉴权后生成短时访问链接
- **周期性/重复自动记账**：房租、订阅费、工资等周期账单
- **账单自由标签**
- **账单收藏或置顶**
- **管理员修改他人账单时的通知**
- 其他未重新确认的扩展功能

## 18. 开发要求

任何开发任务开始前必须：

1. 阅读本文件和 `需求文档.md`
2. 判断任务是否涉及已确认规则
3. 检查当前代码是否已存在相关逻辑
4. 不破坏已有权限、数据隔离和幂等要求
5. 修改云函数时同步检查权限校验
6. 新增页面后在 `app.json` 注册
7. 修改云函数后说明需在微信开发者工具中上传并部署
8. 对初始化、邀请、账单权限和删除操作补充测试
9. 如果需求与本文件冲突，先报告冲突，不得自行覆盖规则
10. 不把"暂缓"功能提前实现为当前版本功能
